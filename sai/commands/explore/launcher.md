The supervised crystallization-close pipeline (explore instructions item 10) dispatches both routed workers from the explore session, so both worker bindings are preloaded here, in the shared launcher.
Fetch @sai/orchestration/workers/bindings/design-worker.md and use it.
