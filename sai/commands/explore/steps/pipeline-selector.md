  10. **Plan - Unattended / Direct Build - Unattended pipeline selector (crystallization-close selector, sai-explore only)**: This is one phase-aware, chat-scoped supervision contract. It does not persist state, discover repository changes, or replace the post-crystallization `review-loop` contract. Explore's manual post-crystallization Review Engine and the supervised in-session Review Engine rounds are the only surfaces that form planning-artifact findings; phase workers have no worker-owned reviewer path. For the selected phase, supervised in-session rounds are the sole automatic convergence mechanism. Explore has no direct write tool; the explicitly user-selected **Plan - Unattended** route may dispatch only the existing `sai-1-spec-proposal-worker` and `sai-2-design-worker` bindings, whose writes remain limited to their selected owned change directory and the artifacts owned by that phase. The explicitly user-selected **Direct Build - Unattended** route may dispatch the existing fast-lane workers under the Build route below. No delegated write outside those scopes is authorized.

    **State contract**: Maintain these values in conversation only:
    - `tracked_changes`: the existing ordered, duplicate-free records in first-emission order. It is the review loop's (item 9) source only, and is never the selector's dispatch source.
    - `last_crystallization_set`: the ordered, duplicate-free `**Change name**` values emitted by the **most recent** crystallization turn (items 5/6/7), in emission order. Each crystallization turn replaces it wholesale; it starts empty and is conversation-only.
    - `completed_changes`: names whose applicable terminal worker returned `completed`; the spec worker is terminal when design is not entered, and the design worker is terminal after spec convergence.
    - `active_change`: the selected name from dispatch until the applicable terminal phase ends; while non-null, another **Plan - Unattended** or **Direct Build - Unattended** selection is acknowledged as already running and dispatches and queues nothing.
    - `active_phase`: `spec`, `design`, or null.
    - `specs_converged_changes`: names whose spec review converged and whose spec artifact gate proceeded; membership survives a failed or cancelled design worker.
    - `auto_answered`: phase-keyed `spec` and `design` ordered in-conversation-only auto-answer records containing exact question, forwarded answer, and grounding citation.
    - `escalated_count`: phase-keyed `spec` and `design` counts, with a running escalated count for each phase.
      - `review_rounds`: one conversation-only object with separate `spec` and `design` counters for the supervised in-session review rounds. Each phase has a budget of three rounds per `plan-unattended` attempt. At the start of each new attempt, reset `review_rounds.spec` and `review_rounds.design` to `0`; each counter counts only completed supervised rounds in that phase during the current `plan-unattended` attempt. Manual-loop reviews (item 9) are separate from supervised rounds and do not count toward either counter, and failed or cancelled attempts do not count toward the new three-round bound.
       - `diagnosis_rounds = { spec: 0, design: 0 }`: one conversation-only object with one counter per phase. Each counter is exactly `0` or `1` for the current `plan-unattended` attempt; reset both counters to `0` at the start of every new `plan-unattended` attempt and increment a phase counter exactly once when that phase's Diagnosis Round runs. These counters are independent of `review_rounds`, never change the ordinary three-round review budget, and are never persisted to worker payloads, journals, artifacts, configuration, or `.openspec.yaml`.
    - **Explore item-10 bounded Diagnosis Round**: On a resolved supervised phase-worker result in the shared **Bounded Recovery** non-clean set — a structurally valid `failed` result (any closed failure class), a coordinator-disproved `completed` result, or a `completed` result carrying STOP — or the Explore-only post-resolution supervised phase-worker `cancelled` result, an unused phase counter first resolves the active phase review item to `pending`, increments once, and invokes `Review Engine(changeName, "sai-1")` or `Review Engine(changeName, "sai-2")` exactly once. Form the ordered `Reported`, `Evidence`, `Cause`, `Correction`, `Verification` record. Review Engine findings are forwarded to the same phase worker with at most one same-worker re-dispatch, expressed as exactly one `continue_after_recovery`, only for an actionable correction and a resumable worker; a cancelled result never becomes a standalone or manual recovery path. Explore must not directly apply, write, edit, or repair the `Correction`, and must not dispatch a replacement worker. An exhausted or failed diagnosis leaves the phase pending and `plan-unattended`-retryable, remains retryable on a later **Plan - Unattended** selection, never dispatches `/sai-3-implement`, and emits only the applicable existing phase guidance (for example, `Next step: run /sai-2-design`). Reference `sai/policies/bounded-recovery.md` as the single shared **Bounded Recovery** contract; must not restate, repeat, or duplicate the full generic ledger or routing contract here. `continuation/transport loss` consumes the diagnosis round and never triggers replacement dispatch. Clean `completed` results (not coordinator-disproved and without STOP), `needs_input`, `progress`, `notice`, every pre-resolution result, and cancellations outside the Explore-only post-resolution supervised phase-worker exception never start diagnosis.
    - **Diagnosis/list-state invariant**: Before the Diagnosis Round after a resolved supervised phase-worker result (including the failed or cancelled item-10 worker case) is either in the shared **Bounded Recovery** non-clean set — a structurally valid `failed` result (any closed failure class), a coordinator-disproved `completed` result, or a `completed` result carrying STOP — or is the Explore-only post-resolution supervised phase-worker `cancelled` result, resolve the active phase review item (`reviewed-sai-1` or `reviewed-sai-2`) to `pending` and render that state change exactly once. While the Diagnosis Round and any possible same-worker re-dispatch are active, neither `reviewed-sai-1` nor `reviewed-sai-2` is set `in_progress` because of diagnosis; no diagnosis-specific list item/row/entry is added. Diagnosis findings do not mark or clear review evidence and are not counted as a Supervised Review Round; `diagnosis_rounds` does not affect review-round list state. If a successful same-worker re-dispatch resumes the ordinary supervised review lifecycle and reaches ordinary supervised review entry, only that ordinary review entry sets the normal phase item `in_progress`; diagnosis itself does not. If diagnosis stops, re-dispatch fails, or the worker fails/cancels again, the phase item remains/resolves to `pending`. Diagnosis counters/findings/render state are conversation-only and never persisted to files, artifacts, config, the change directory, or `.openspec.yaml`. These rules leave panel ownership, machine-readable identities, no-persistence, the existing clean lifecycle, and the Build boundary unchanged.
  - `overview_language`: the selected language token for the crystallized idea or slice set, or the literal `None` when overview creation is declined, resolved non-committally, or bypassed by fast-track. A non-`None` value is the sole overview opt-in signal: it is selected by gate 9 at supervised **Plan - Unattended** activation or supplied by an explicit `--overview-lang` value. This value is conversation-only and chat-scoped per crystallized idea or slice set; it survives a failed or cancelled supervised attempt so a later design retry can reuse it, and it resets when a materially new idea is explored. It is never written to any file.

     Spec and design review rounds and autonomy records remain separate. State is chat-scoped, in-conversation-only, and is never read from or written to repository state. A diagnosis-eligible non-clean result leaves the change uncompleted and retryable at that worker's phase. Cap exhaustion is non-failure and continues the run; only a result in the shared **Bounded Recovery** non-clean set or the Explore-only post-resolution supervised phase-worker `cancelled` exception interrupts it.

      **Crystallization-close selector**: There is no literal pipeline token. Route execution is entered **only** through this selector, and no token form is recognized. Every crystallization emission — items 5, 6, and 7 — closes its turn with exactly one selector by invoking **Crystallization-turn close (shared)** above exactly once; this selector is that close's final emission, after the final `Ready to Propose` block and after the keep-window-open recommendation. Present it through the harness-native picker per `sai/policies/remember.md` (L10–15): `AskUserQuestion` on Claude Code, the `question` tool on opencode. Only that native picker call counts as the selector — a plain-text list does not count. It carries exactly three options, in this fixed order, each carrying its fixed one-line description, with fixed English titles `Plan - Unattended`, `Direct Build - Unattended`, `Manual`. The stable machine-readable route identities are exactly `plan-unattended`, `direct-build-unattended`, and `manual`; they are not localized and are never accepted as compatibility aliases:

      - **Plan - Unattended** — Runs `sai-1` and `sai-2` to create the plan and stops for pre-implementation human review.
      - **Direct Build - Unattended** — Implements the change directly and updates specs afterward: ideal for fixes and simple changes.
      - **Manual** — Proceed manually by pasting the `Ready to Propose` block into a new chat with `/sai-1-spec` (full control over the process).

   Selecting **Plan - Unattended** (`route_mode = plan-unattended`) delegates supervised `sai-1` + `sai-2` execution to this session under the contract below. Selecting **Direct Build - Unattended** (`route_mode = direct-build-unattended`) runs the unattended code-first fast lane under the **Direct Build (unattended) supervision contract** below: direct block-driven implementation, functional fix loop, backfill preparation followed by coordinator-authorized draft execution, archive preparation followed by coordinator-authorized archive execution, and the pre-authorized local commit in one run. Neither dispatching option loads or dispatches anything unless selected; **Manual** (`route_mode = manual`) and every other command surface are unaffected by the selector.

Do not fetch `pipeline-plan-unattended.md` or `pipeline-direct-build.md` at selector presentation. On a **Plan - Unattended** selection, POST `/emit` with `{machineId: "explore-slice@1", eventId, event: {intent: "plan"}}`. On a **Direct Build - Unattended** selection, POST `/emit` with `{machineId: "explore-slice@1", eventId, event: {intent: "direct-build"}}`. If the response carries `rejected: ALREADY_RUNNING`, acknowledge already running and dispatch nothing. After a non-rejected emit, consume `next.follow` under the spawn/emit fetch-follow contract — load only when this chat's conversation loaded-set does not already contain that path. Plan loads `pipeline-plan-unattended.md` at sai-1; Direct Build loads `pipeline-direct-build.md` at build-implement.

   After the selector response, when diagnosis is exhausted or fails, emit only the existing phase guidance: `Next step: run /sai-1-spec` for spec or `Next step: run /sai-2-design` for design; this is the **Diagnosis-stop guidance** and does not emit a second next-step handoff.

    The routed design worker remains name-first and accepts either flag order after the name. Preserve the inverse supervised order for retries: `arguments_value: "{name} --supervised --fast-track"`; when a non-`None` overview language is selected, its inverse form is `arguments_value: "{name} --supervised --fast-track --overview-lang {overview_language}"`. The canonical Plan (unattended) envelope and the no-language rule below remain unchanged.

     The question text and each option description render in the user's language per item 8; the fixed option titles remain exactly `Plan - Unattended`, `Direct Build - Unattended`, and `Manual`. The literal `review-loop`, `/sai-1-spec`, and `/sai-2-design` command strings stay verbatim English. Selecting **Plan - Unattended** is the explicit user act that authorizes item 1's delegated-write exception; it is consent to selection and dispatch only and is never consent to answer a later worker question. Selecting **Direct Build - Unattended** is likewise the explicit user act that authorizes item 1's delegated-write exception for the implementer, backfill, and archive workers AND pre-authorizes exactly one local commit executed by the existing `sai-archive-worker` inside its coordinator-validated closed order; it, too, is consent to selection and dispatch only. Selecting **Manual** (`route_mode = manual`) dispatches nothing, changes no state value, injects no plan, build, fast-track, or overview-language marker, and refers to the one keep-window-open recommendation already emitted by the **Crystallization-turn close (shared)** before the selector (naming `review-loop` exactly once). It MUST NOT emit a second recommendation or selector for the same answer. After the selector response, the surrounding prose of the path-specific Manual/unmapped next-step handoff follows the selected crystallization language, while its command and standing-path literals remain verbatim English; emit that handoff **exactly once**, choosing the wording that matches the crystallization path that just closed:

  - **Single-change path**: emit the existing `**Open a new chat**` instruction and run `/sai-1-spec` with the content above.
  - **Sliced path**: Tell the user to take the **first** block to a new chat with `/sai-1-spec`, and that each later slice becomes its own change once its predecessor is specced (the "new change per follow-up" pattern).
  - **Inline-refusal path**: emit the existing handoff: "Creating a proposal opens a new context. The paste-ready block follows — copy it and start a new chat with `/sai-1-spec` to keep the spec session clean." Do not dispatch a proposal in-session.

    A free-text answer that maps to neither option is treated as **Manual** (`route_mode = manual`) and follows the same no-dispatch, no-state-change, no-injected-marker, no-second-recommendation, and no-second-selector rules; the path-specific next-step handoff is emitted exactly once after the selector response for every Manual/unmapped answer. **Manual is not terminal** — re-emit the selector whenever the user asks for it (a turn whose dominant intent is to run the **Plan - Unattended** or **Direct Build - Unattended** route or to see the selector again; no token form exists and none is recognized), with no cap on re-emissions. A re-emission after a missing-selector report uses the current chat `last_crystallization_set` only, without re-emitting the recommendation and without dispatching a stale change. Neither the item-8 crystallization language gate nor `--fast-track` exempts the selector. `--fast-track` auto-selects nothing: the selector is always asked, and it is the one gate to delegated writes that fast-track never skips.

    **Deterministic selection**: On a **Plan - Unattended** or **Direct Build - Unattended** selection, use only `last_crystallization_set` and `completed_changes`:
    - If `last_crystallization_set` is empty, acknowledge that no change has been crystallized in this chat and dispatch nothing.
    - If no uncompleted entry remains, acknowledge that every change of the last crystallization turn is already completed and dispatch nothing.
    - If exactly one uncompleted entry remains, dispatch that sole change without a picker; no intermediate picker is presented.
    - If multiple uncompleted entries remain, present one harness-native ordered picker containing the uncompleted names in emission order followed by `Cancel`. Dispatch at most one selected entry.
      - If the user selects `Cancel`, dispatch nothing and leave every state value unchanged.
       - Never run `openspec list --json`, enumerate changes on disk, infer repository changes, or re-sort the emitted names. Diagnosis-eligible non-clean attempts remain retryable because they never enter `completed_changes`.
    **Item 10 cancellation path**: A resolved post-resolution supervised phase-worker `cancelled` result uses this Explore-specific bounded diagnosis route and never becomes a standalone or manual recovery path; cancellations outside this Explore-only route remain excluded.
      - A later crystallization turn replaces `last_crystallization_set` with that turn's emitted names, and that replacement also invalidates each re-emitted name's prior lifecycle state: every name in the new set is removed from `completed_changes` and `specs_converged_changes`, so a re-crystallized change re-enters supervised execution at the spec phase over its new block instead of being acknowledged as already completed or routed into a design-phase retry over superseded specs. Blocks emitted by earlier crystallization turns are assumed applied or discarded, are no longer arbitrated by the selector, and remain reachable only by running `/sai-1-spec` by hand. Because the prior state is no longer arbitrated, an in-flight worker — which occupies the turn — is the only conflict the run rejects, through the active-supervision rule below.
    **Mode-specific route projection.** The selector's resolved slice and route initialize a conversation-only `active_route` record for the selected slice. The record contains `route_mode` (exactly `plan-unattended`, `direct-build-unattended`, or `manual`), `route_slice`, the fixed ordered `route_steps`, each step status, and the current `active_step`; it is not an invocation envelope field, worker payload field, artifact field, or persisted state. Render the route through item 11's coordinator-owned idea-list projection before the selected route's first delegated dispatch or handoff.

     - A **Plan - Unattended** answer (`route_mode = plan-unattended`) initializes exactly `sai-1`, `sai-2`, and `Implement`, with `sai-1` `in_progress` and `sai-2` and `Implement` `pending`, then dispatches the existing supervised spec worker. A clean spec convergence changes only the route projection to `sai-1` `completed` and `sai-2` `in_progress` before the chained design dispatch. A clean terminal design result completes `sai-2` and starts `Implement` as `in_progress`; it does not claim that `sai-3` or code implementation ran.
     - A **Direct Build - Unattended** answer (`route_mode = direct-build-unattended`) initializes exactly `Build/Implement`, `Backfill`, and `Archive`, with `Build/Implement` `in_progress`. The display label `Build/Implement` is a Build-route label only and is never the `/sai-build` command or the `meta-build` composition. The underlying eight-step Direct Build (unattended) contract remains authoritative while the panel shows only these three high-level stages.
    - A **Manual** or unmapped answer (`route_mode = manual`) initializes only `Manual handoff`, emits the existing path-specific handoff including the executable `/sai-1-spec` block-paste example (paste the `Ready to Propose` block, not just the name), and then marks that one handoff `completed`. It changes no supervision state value, creates no delegated route steps, dispatches no worker, and does not inject any plan, build, fast-track, or overview-language marker.

    The route projection is temporary: it substitutes for the selected slice's three baseline evidence entries and never duplicates or marks `slice-crystallization`, `reviewed-sai-1`, or `reviewed-sai-2`. It is scoped to the selected slice, so other slices retain their own states. A failed, cancelled, STOP-bearing, coordinator-disproved, or unrecovered result leaves the active route step `pending` and retryable, preserves earlier completed route steps, and starts no later route step; only a clean terminal result completes the route. Route state remains conversation-only, uses the existing slice marker and panel owner, is emitted by the coordinator only, and produces no stamps. Claude Code and opencode consume this shared route contract through their existing idea-list bindings; no route semantics are duplicated in either harness binding.
