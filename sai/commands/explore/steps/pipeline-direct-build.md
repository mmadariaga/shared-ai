   **Direct Build (unattended) supervision contract**: The third selector option runs one unattended code-first flow for the currently selected slice, ending with an archived, committed change in one session. A sliced crystallization set is therefore handled as a sequence of independently completed slice runs: each run enters through the existing selector and executes the same fixed worker order, while the completion transition below decides whether another slice-selection turn is needed. A single-slice run retains its existing terminal behavior. It composes exclusively existing contracts — the three-round budget semantics, **Bounded Recovery**, the archive fast-track gates, and `@sai/policies/commit-rules.md` faithfulness rules — and exists beside, never instead of, the Plan (unattended) contract above; its stable route identity is `direct-build-unattended`. Selecting it consents delegated writes (item 1) AND pre-authorizes the local commit; it is consent to selection and dispatch only.

   **Direct Build `--no-specs` profile (viability POC).** The uncertainty pause (crystallization protocol) may dispatch a POC via Direct Build with the `--no-specs` flag. This profile runs the implementer only — no backfill, no OpenSpec specs, no artifact archive. The POC is a disposable viability probe: dirty code is acceptable, minimal tests or human review are the verdict authority, and no OpenSpec trail is created for the POC itself. The `--no-specs` profile executes only Steps 1 and 2 of the eight-step Direct Build flow (implement + functional fix loop), then returns the verdict (viable or not viable) to the uncertainty pause machinery for post-POC menu presentation. Steps 3–8 (backfill, spec review, ADR/DDR, archive) are skipped entirely. The POC does not create or modify anything under `openspec/`, does not create planning artifacts, and does not run mutating git commands. The implementer's `changed_files` union is returned to the uncertainty pause for verdict assessment; the POC code is disposable and no mandatory auto-delete occurs on viable+crystallize.

   - **Deterministic selection inheritance**: On a **Direct Build (unattended)** selection (`route_mode = direct-build-unattended`), apply the **Deterministic selection** rules above verbatim against `last_crystallization_set` and `completed_changes`, including every degenerate selection state: an empty set acknowledges nothing crystallized and dispatches nothing; no uncompleted entry acknowledges completion and dispatches nothing; exactly one uncompleted entry dispatches without a picker; multiple entries use the same ordered native picker with `Cancel`; and an active run rejects another selection for the full interval (acknowledge already running, dispatch and queue nothing). Never run `openspec list --json` or re-sort names. This is also the authoritative slice-selection path used when the completion transition re-enters the selector; it never selects a name already in `completed_changes`. Where earlier clauses describe this selector as carrying two options or both option labels (items 8 and 9), those descriptions are superseded for count only by this third option; their localization, English-literal, and no-auto-select semantics carry over unchanged.
     - **Run state** (conversation-only, never persisted): reuse `active_change`; add `base_sha` (the repository HEAD captured immediately before the implementer dispatch), `fix_rounds` (a counter with the existing `review_rounds` semantics: at most three completed rounds per attempt, reset on a new attempt), `direct_build_mode` (`prepare` or `execute` for the active backfill/archive worker), `validated_execution_order`, `execution_state` (`pending`, `executed`, or `failed`), and the `direct_build_commit_authorized` flag consumed only by the archive execute continuation. Add `diagnosis_rounds.direct_build = { direct-build: 0 }` following the bounded Diagnosis Round counter rules. Each re-entered slice selection starts a fresh slice attempt and resets these per-attempt values, while `completed_changes` and completed slice progress states remain intact. No progress plan is declared; no plan-based list renders; the idea progress list (item 11) keeps panel ownership.
   - **Pre-dispatch compatibility refusal and recovery**: before Step 1, judge the emitted block against the step-1 implementer's work scope only (steps 3–8, backfill and archive, own writes to OpenSpec bookkeeping artifacts under `openspec/changes/{name}/**` and `openspec/specs/**`). Reading ONLY the block, check for genuine step-1 violations: planning-artifact creation (`design.md`, `tasks.md`, `implementation.md`), mutating git commands, subagent dispatch, or writes under `openspec/` that the block designates as step-1 implementation work (including non-bookkeeping content like `openspec/schemas/**` or `openspec/config.yaml`). When the block evidence clearly triggers a genuine violation, emit a documented refusal naming the violated clause and the block evidence, show it to the user, dispatch nothing, mutate no selection state, and leave the change retryable; Manual stays available. When a block's `**Capabilities in scope**` or `**Implementation Details**` designate implementation work targeting non-bookkeeping content under `openspec/`, enter a pre-dispatch recovery cycle bounded by a round cap (at most two correction attempts): present the incompatibility naming the violated clause and the block evidence, allow the block to be corrected in session memory, revalidate against this narrowed refusal, and proceed to Step 1 with the corrected block when revalidation passes. If correction attempts are exhausted or declined, emit the documented refusal with the last-attempted block state as evidence, show it to the user, dispatch nothing, mutate no selection state, and leave the change retryable; Manual stays available. The correction is session-scoped: it writes no file, does not rewrite the block already printed in the chat history, keeps explore read-only, and changes only the `arguments_value` travelling to the implementer; it creates no `task_id`, consumes no `fix_rounds`, and does not touch Bounded Recovery. A block that only mentions or references OpenSpec bookkeeping artifacts (in `**Why**`, `**Decisions & Rationale**`, or `**Research Leads**`) passes straight through to Step 1 with no refusal and no recovery, as steps 3–8 absorb those artifacts. Bounded Recovery remains post-dispatch only and never substitutes for this refusal.
    - **Eight steps, fixed order**:
       Fetch @sai/orchestration/workers/bindings/direct-build-worker.md and use it.
      - **No-commit guard windows**: every dispatch of this run is guarded by the deterministic no-commit guard — Fetch @sai/policies/no-commit-guard.md and follow it exactly. The run has three guard windows: (1) the implementer window, opened by the Step 1 snapshot whose head is recorded as BOTH `base_sha` and the window's `guard_base`, verified after the implementer stretch closes — the fix loop converged or the cap exhausted — and before the Step 3 staging; (2) the backfill window, opened by a fresh snapshot immediately before the Step 3 prepare dispatch — taken after the path-scoped staging, so the coordinator's `git add` runs outside every window — and verified after the Step 6 execute terminal before acting on it, with the findings and execute continuations each opening their own sub-window from a fresh snapshot and verified after their result; (3) the archive window, opened by a fresh snapshot immediately before the Step 7 prepare dispatch, with a fresh sub-window snapshot immediately before the Step 8 execute continuation whose verify runs with `--allow-commit`, because that continuation's validated closed order contains the one pre-authorized local commit. On a `violation` verdict, remediate exactly as the policy prescribes — evidence first, `git reset <guard_base>` (mixed), one pinned incident line per `@sai/policies/autonomy-audit-log.md`, then continue the route.
      1. **Implement** — capture `base_sha` by running the guard's `snapshot` step (the snapshot's head is `base_sha` and the implementer window's `guard_base`), then dispatch the `sai-direct-build-worker` through its binding with the one-string envelope whose `arguments_value` is the marker line `--direct-build` + newline + the complete emitted `Ready to Propose` block (`α` input model: the worker receives ONLY that block — no conversation context, no inferred requirements). Accept only closed lifecycle statuses; union returned `changed_files` first-seen.
     2. **Functional fix loop** — review the resulting diff (`git diff` from `base_sha`) against the block's Capabilities and Edge Cases: run the project's checks when available (standard test/lint/build scripts detected from project metadata), otherwise a structural check (every capability maps to changed paths; every Edge Case is addressed or explicitly contradicted by a constraint). Form findings under `@sai/policies/artifact-review-contract.md`. A round with findings continues THE SAME implementer worker with exactly the ordered finding list and increments `fix_rounds`; a round with no findings converges. A third completed round carrying findings is non-failure cap exhaustion (E1): emit one line with the last round's exact base-form tally, report it, and continue forward without further rounds.
       3. **Sibling backfill preparation** — stage exactly the implementer's owned paths (path-scoped `git add`; unrelated dirty files never enter), then dispatch the EXISTING `sai-backfill-worker` through its existing binding and Direct Build (unattended) prepare route with `arguments_value` beginning `--direct-build-prepare`, followed by `--fast-track --staged` and the complete emitted `Ready to Propose` block. Fetch @sai/orchestration/workers/bindings/backfill-worker.md and use it before this dispatch. The dispatch requests the backfill worker to compose the draft set including `openspec/changes/{name}/.openspec.yaml` in the canonical form specified at `sai/commands/backfill/instructions.md` section 6a, in addition to `proposal.md` and capability specs. Interview asks resolve through the generic grounded auto-answer-or-escalate machinery — same qualitative confidence threshold, ambiguity resolving toward escalation, permitted grounding extended only by this run's block, fix-loop findings, staged diff summary, and captured `base_sha`: the diff-source ask resolves to the staged-changes option grounded in `base_sha`; Question 1 grounds in the block's What/Why; Question 2 grounds in the fix-loop findings; anything ungrounded escalates unchanged. A foreign-spec conflict auto-proceeds per the delta-sync/fast-track precedent (E2), reported as a notice. Preparation performs no draft write.
     4. **Spec review** — explore validates the returned draft CONTENT read-only against `openspec/schemas/sai-workflow/schema.yaml` (pure reading — the validation/writer split: explore writes nothing) and accepts `openspec/changes/{name}/.openspec.yaml` as part of the returned draft set, validating it as the canonical three-key form from `sai/commands/backfill/instructions.md` section 6a (schema, created as YYYY-MM-DD date only, backfilled: true) rather than against the capability schema. Reviews the drafts against the block under `@sai/policies/artifact-review-contract.md`, and continues any findings once to the SAME backfill worker within the remaining `fix_rounds` budget; schema-invalid or still-failing drafts route through Bounded Recovery below.
   MODIFIED-delta completeness: the schema review also compares each draft `## MODIFIED Requirements` scenario set read-only against the existing main spec at `openspec/specs/{capability}/spec.md` — a MODIFIED that drops an existing scenario is a High completeness finding routed through the same findings continuation, because syncing it would silently destroy content.
        5. **ADR/DDR pass** — fetch `@sai/policies/adr-ddr-criteria.md` and run its ordered routing test over the block's Decisions & Rationale; never offer a choice. When `adr|ddr` resolves, record the resolved family and pass outcome in one line and proceed. The Direct Build (unattended) route does not add a decision-record file to the backfill artifact set: that set is restricted to the schema-validated OpenSpec drafts plus the `.openspec.yaml` metadata file in the canonical form specified at `sai/commands/backfill/instructions.md` section 6a, and persistent ADR/DDR creation remains owned by the normal implementation flow.
       6. **Backfill execution** — after the schema validation and ADR/DDR decisions above, continue the SAME `sai-backfill-worker` with exactly one `--direct-build-execute` closed order containing only the validated draft paths and byte-for-byte contents, including `openspec/changes/{name}/.openspec.yaml`. This continuation is the coordinator's explicit execution authorization; the worker writes the drafts and nothing else. It stops before archive preparation.
       Fetch @sai/orchestration/workers/bindings/archive-worker.md and use it.
        7. **Archive preparation** — dispatch the EXISTING `sai-archive-worker` through its binding with an `--direct-build-prepare` envelope for the resolved name, declaring `fast_track_active` and `direct_build_mode: prepare` as session state alongside the envelope. Run the read-only pre-flight over the materialized artifacts and resolve the unchecked-items gate under the existing fast-track semantics — unchecked-items always auto-proceeds — presenting each resolved gate as a notice. The worker returns the validated CLI archive, staging, and commit plan without mutating.
        8. **Archive execution and pre-authorized commit** — after coordinator validation, gate resolution, and the one-commit authorization state are all present, continue the SAME `sai-archive-worker` with exactly one `--direct-build-execute` closed order. The worker runs `openspec archive <name> --yes --json` as the sole sync + move primitive, stages only the supplied OWNED paths (reconstructed artifacts, synced main specs, and the implementer's unioned changed paths — unrelated dirty files never enter the commit, E4), authors the commit message from staged state under `@sai/policies/commit-rules.md`, and performs the HEREDOC local commit; never push, amend, retry, or add an action outside the order.
       - **Failures** (E3): every segment applies `@sai/policies/bounded-recovery.md` **Bounded Recovery** verbatim where recovery is allowed by this supervision contract — one diagnosis record and at most one same-worker recovery continuation. A backfill execution failure with a partial mutation reports the exact draft paths written before stopping and never refires any order onto the partially mutated state. An archive preparation or execution failure evaluated as a backfill-artifact error routes the verbatim error to the same backfill worker that created those specs for correction and relaunches archive with the corrected artifacts. A repeated defect reported without progress after correction closes as failed-retryable with the verbatim failure in view and no further automatic continuation. A late continuation after success is rejected without mutation. Repeated-defect and partial-mutation closures carry no finality: new retries or changes run only at explicit user request. An archive preparation or execution failure that is not a backfill-artifact error reports the exact CLI/staging/commit state and never commits a partial plan. Manual `/sai-backfill`, `/sai-archive`, and `/sai-commit` guidance is reported as applicable.
      - **Terminal report**: report rounds used, any cap tallies, the autonomy audit assembled from this run's auto-answer and escalation records in the pinned layout of `sai/policies/autonomy-audit-log.md` (with this run's label as the phase label), print the worker-authored summaries verbatim, close naming the archive destination and commit subject, and clear `active_change`. Only a **Bounded Recovery** non-clean result interrupts the run; cap exhaustion is non-failure and continues forward.
     - **Successful slice completion transition**: After the final archive-worker execution returns a clean terminal `completed` result, record its `changed_files`, add `active_change` to `completed_changes`, preserve completed slice progress states, and recompute `last_crystallization_set - completed_changes` in crystallization order. Re-enter the selector only when uncompleted slices remain; otherwise emit terminal navigation exactly once. Failed, cancelled, incomplete-recovery, coordinator-disproved, or STOP-bearing results do not mark a slice completed or re-enter selection.
      1. Record the terminal result's `changed_files` in the existing first-seen union and add the selected `active_change` to `completed_changes` **before** deciding the next action.
      2. Preserve the completed slice's progress states in the idea progress list; do not clear, rewind, or reclassify its completed entries. Then end that slice's active interval without changing any worker payload or commit ownership.
      3. Recompute `pending_slices` only from `last_crystallization_set` minus `completed_changes`, preserving crystallization order. If at least one pending slice remains, re-present the existing full three-option `Plan (unattended)` / `Direct Build (unattended)` / `Manual` selector exactly once. This continuation selector is a per-slice authorization gate, not a one-time authorization for the entire crystallized set: show it even when exactly one pending slice remains, and do not start that slice until the user makes a new selection. The transition itself dispatches no worker; the re-presented selector remains the consent and selection point for the next independent slice run.
         - For a `Plan (unattended)` or `Direct Build (unattended)` continuation choice, apply the existing deterministic slice-selection path only to `pending_slices`: a single pending slice dispatches without an intermediate name picker, while multiple pending slices use the ordered native picker and `Cancel`. Never re-select or re-run a name already in `completed_changes`, and never re-sort the crystallization order.
         - Selecting `Manual` on this continuation selector starts no additional slice: dispatch nothing, leave `pending_slices` and the completed-slice state unchanged, and require a later explicit request before any pending slice runs. This continuation `Manual` answer does not re-emit the crystallization-turn handoff or the exhausted-set terminal report/navigation; the initial crystallization-close `Manual` branch remains unchanged.
      4. If no uncompleted slice remains, show no selector and continue to the existing terminal report and navigation exactly once. This exhausted-set branch is also the single-slice branch, so a single-slice run has no additional prompt or observable terminal change.
      This transition is never entered for a `failed`, `cancelled`, or incomplete-recovery outcome, nor for a non-clean `completed` result that is coordinator-disproved or carries `STOP`; those outcomes retain the existing **Bounded Recovery** and retry behavior and never mark the slice completed or re-enter selection. A functional-fix cap is non-failure and continues the fixed order; it is not itself a completion transition.
      - **Failures** (E3): every segment applies `@sai/policies/bounded-recovery.md` **Bounded Recovery** verbatim where recovery is allowed by this supervision contract — one diagnosis record and at most one same-worker recovery continuation. A backfill execution failure with a partial mutation reports the exact draft paths written before stopping and never refires any order onto the partially mutated state. An archive preparation or execution failure evaluated as a backfill-artifact error routes the verbatim error to the same backfill worker that created those specs for correction and relaunches archive with the corrected artifacts. A repeated defect reported without progress after correction closes as failed-retryable with the verbatim failure in view and no further automatic continuation. A late continuation after success is rejected without mutation. Repeated-defect and partial-mutation closures carry no finality: new retries or changes run only at explicit user request. An archive preparation or execution failure that is not a backfill-artifact error reports the exact CLI/staging/commit state and never commits a partial plan. Manual `/sai-backfill`, `/sai-archive`, and `/sai-commit` guidance is reported as applicable.
      - **Terminal report**: report rounds used, any cap tallies, the autonomy audit assembled from this run's auto-answer and escalation records in the pinned layout of `sai/policies/autonomy-audit-log.md` (with this run's label as the phase label), print the worker-authored summaries verbatim, close naming the archive destination and commit subject, and clear `active_change`. Only a **Bounded Recovery** non-clean result interrupts the run; cap exhaustion is non-failure and continues forward.
       **Slice-machine emits.** On a **Direct Build (unattended)** selection, POST `/emit` with `{machineId: "explore-slice@1", eventId, event: {intent: "direct-build"}}`. If the response carries `rejected: ALREADY_RUNNING`, acknowledge already running and dispatch nothing. After each high-level route item converges (`Build/Implement`, then `Backfill`, then `Archive`), POST `/emit` with `{machineId: "explore-slice@1", eventId, event: {intent: "complete"}}`. Completing Archive is the machine's done signal: it marks the slice done and clears `active`. Direct Build never emits `next-slice`. Fail/cancel leaves the active step pending and does not mark the slice done.

    **`direct-build-unattended` route projection lifecycle.** The selected slice's temporary Build route is initialized before Step 1 and is rendered by the explore coordinator as exactly three high-level items: `Build/Implement`, `Backfill`, and `Archive`. `Build/Implement` starts `in_progress`; after the implementer and functional fix loop converge, it becomes `completed` and `Backfill` becomes `in_progress`; after clean backfill execution, `Backfill` becomes `completed` and `Archive` becomes `in_progress`; after the final archive execution and pre-authorized commit return a clean terminal `completed`, `Archive` becomes `completed`. The route label `Build/Implement` is not `/sai-build` and does not mean that the `/sai-build` or `meta-build` composition ran.

   The eight internal Build-route steps remain authoritative, but functional review, spec review, ADR/DDR checks, preparation, execution, and commit are substeps of these three route items and never become separate panel entries. The route is not a routed `progress_plan`; it is the coordinator-only idea-list projection for the selected slice and replaces that slice's baseline evidence entries without duplicating them. A failed, cancelled, STOP-bearing, coordinator-disproved, or unrecovered result leaves the active high-level route item `pending`, preserves earlier completed route items, starts no later item, and leaves the slice retryable. Only the clean terminal archive result completes the route. A new slice selection creates fresh route state for that slice; completed slices and pending slices retain their independent states.
