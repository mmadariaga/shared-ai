# Routing: GREEN Exception — No Production

**RED block present + exact unambiguous `## Step N` contract + no production file** → dispatch exactly one RED green-exception (`test-authoring → green-verification`) that terminates with GREEN = pass.
