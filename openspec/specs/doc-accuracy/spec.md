# doc-accuracy Specification

## Purpose
Define the model and effort assignments AGENTS.md and the README must state for the Build, Meta-review, Design, and Spec coordinator/worker routing lines.

## Requirements

### Requirement: AGENTS.md SHALL match wrapper-plus-matrix model and effort assignments
Scope covers the Build, Meta-review, Design, and Spec coordinator/worker lines.
#### Scenario: Maintainer reads AGENTS.md routing table
- **WHEN** a maintainer reads the Build, Meta-review, Design, or Spec routing line
- **THEN** AGENTS.md states opus medium effort and muse-spark-1.3-contributor xhigh with medium-effort coordinator and medium-effort design worker

### Requirement: README SHALL match wrapper-plus-matrix model table and design line
Scope covers the README design bullet and the explore, implement, apply, build, and sai-review model table rows plus the pinned design test literal.
#### Scenario: Maintainer reads README model table
- **WHEN** a maintainer reads the README design bullet or model table
- **THEN** README states opus medium alignment and muse-spark-1.3-contributor xhigh for the affected rows
