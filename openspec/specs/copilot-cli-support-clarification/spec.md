# copilot-cli-support-clarification Specification

## Purpose
TBD - created by archiving change minor-copilot-changes. Update Purpose after archive.
## Requirements
### Requirement: The README SHALL explicitly list Copilot CLI as an unsupported surface for shared-ai

The README.md Copilot compatibility statement MUST mention that Copilot CLI is not supported, in addition to the existing exclusions (agents window preview).

#### Scenario: User reads Copilot support scope in README
- **WHEN** a user reads the Copilot support paragraph in README.md
- **THEN** the text clearly states that Copilot CLI is not a supported surface alongside the agents window (preview) exclusion

