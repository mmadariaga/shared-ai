## MODIFIED Requirements

### Requirement: Apply phase Copilot model metadata SHALL use GPT-5.4 mini identifiers

The apply-phase model entry in command metadata and mirrored documentation MUST reference the GPT-5.4 mini Copilot identifiers.

#### Scenario: apply-phase model metadata is updated
- **WHEN** maintainers define or display the apply-phase Copilot model in the command wrapper and model matrix documentation
- **THEN** they use `GPT-5.4 mini (copilot)` and `github-copilot/gpt-5.4-mini`
