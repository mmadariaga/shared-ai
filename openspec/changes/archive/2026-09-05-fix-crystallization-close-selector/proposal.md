> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: low (prompt-only hardening, 2 files, no breaking change)

## Why
A single-change transcript ended after the recommendation without a `question` call, leaving no Plan / Direct Build / Manual choice. This change hardens the crystallization-close so every crystallization turn emits the pipeline selector after the recommendation, with evidence in `sai/commands/explore/instructions.md:45-51`, `sai/commands/explore/steps/crystallization-protocol.md:30`, and `sai/commands/explore/steps/pipeline-selector.md:20`.

## What Changes
- `sai/commands/explore/steps/crystallization-protocol.md`: added emission guarantee forbidding ending the turn after the recommendation without the native-picker selector call carrying the three exact titles, and added the slice-set shared-close pointer with the same three titles.
- `sai/commands/explore/steps/pipeline-selector.md`: clarified only the native picker call counts as the selector with fixed English titles, clarified re-emission uses the current chat `last_crystallization_set` without recommendation re-emit and without stale dispatch, and clarified the language gate and fast-track never exempt the selector.
- Preserved: Plan / Direct Build supervision contracts unchanged; fast-track still never auto-selects.
- Accepted: minimal duplication of the three titles against MUST NOT restate to favor reliability.
- No code changes; prompt hardening only across two files, 5 insertions and 3 deletions.

## Capabilities
### New Capabilities
- crystallization-close: guarantee selector emission via native picker after recommendation on every 5/6/7 emission

### Modified Capabilities
- None

## Impact
- Modified: `sai/commands/explore/steps/crystallization-protocol.md`
- Modified: `sai/commands/explore/steps/pipeline-selector.md`
- New files: none
Out-of-scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
