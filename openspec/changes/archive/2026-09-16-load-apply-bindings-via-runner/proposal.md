> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: low (1 file, load directive, no breaking change)

## Why
The apply coordinator dispatches through the active apply binding but no Fetch loads it. The chained activation list excludes the bootstrap while composition never re-enters wrappers, so runner.md is the only single directive covering both standalone and chained paths. A stale build path literal in sai-build-registration names a path that does not exist on disk.

## What Changes
- sai/commands/apply/runner.md now loads the RED worker binding and the GREEN worker binding through one neutral Fetch each, covering standalone and chained apply dispatches.
- The apply bootstrap stays empty, no manifest or matrix rows change, card selection is unchanged, and green-exception dispatches use the existing active binding.
- The staged implementation evidences only the runner wiring change; the registration literal correction has no staged evidence and is excluded from normative content in this run.

## Capabilities
### New Capabilities
- apply-dispatch-wiring: RED/GREEN dispatch wiring loads on standalone and chained apply paths via runner.md.

### Modified Capabilities
- None.

## Impact
- Modified: sai/commands/apply/runner.md (2 Fetch directives, 3 insertions, 0 deletions).
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
