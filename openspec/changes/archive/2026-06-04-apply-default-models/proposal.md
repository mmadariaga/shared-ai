> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

## Why

The model reference table in README.md was missing a row for `sai-backfill`, leaving its recommended models undocumented. Additionally, the Copilot model identifier for `sai-4-apply` was stale (`GPT-5 mini`) and needed updating to the correct version (`GPT-5.4 mini`), both in the documentation table and in the Copilot command wrapper frontmatter.

## What Changes

- `README.md` — corrected `apply (4)` Copilot model identifiers from `GPT-5 mini (copilot)` / `github-copilot/gpt-5-mini` to `GPT-5.4 mini (copilot)` / `github-copilot/gpt-5.4-mini`
- `README.md` — added `backfill` row to the model table with opencode-go model `qwen3.6-plus`, Claude model `claude-sonnet-4-6`, and Copilot model `Claude Sonnet 4.6 (copilot)` / `github-copilot/claude-sonnet-4.6`
- `commands/copilot/sai-4-apply.prompt.md` — updated `model:` frontmatter from `GPT-5 mini (copilot)` to `GPT-5.4 mini (copilot)`

## Capabilities

### New Capabilities

- `backfill-model-assignment`: documents the default models assigned to the sai-backfill command across all supported harnesses.

### Modified Capabilities

- `copilot-apply-model-correction`: corrects the Copilot model identifier for sai-4-apply from GPT-5 mini to GPT-5.4 mini in both the README table and the command wrapper frontmatter.

## Impact

- **Modified files**: `README.md`, `commands/copilot/sai-4-apply.prompt.md`
- **New files**: none
- **Out of scope**: `design.md`, `tasks.md`, `implementation.md` — not generated by `/sai-backfill`
