**Complexity**: medium (3 changed files, 2 modified capabilities, no new dependency)

## Why

> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

The Auto terminal previously reported `sai-3 was not run.` without providing the composition-aware next command. The staged change directs successful Auto completion to `/sai-build {name}`, while standalone design navigation remains `/sai-3-implement {name}`.

## What Changes

- Successful Explore Auto completion emits `Next step: run /sai-build {name}.`.
- The obsolete `sai-3 was not run.` terminal text is removed.
- Contract-preservation and pipeline-selector tests verify the new handoff.
- Standalone `/sai-2-design` navigation remains unchanged.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `explore-pipeline-selector`: successful Auto completion hands off to the build composition.
- `pipeline-design-phase-chaining`: chained design completion reports the composition-aware next step.

## Impact

- Modified `sai/commands/explore/steps/pipeline-auto-supervised.md`.
- Modified `test/explore-contract-preservation.test.js`.
- Modified `test/explore-pipeline-selector.test.js`.
- Preserved standalone navigation in `sai/commands/design/coordinator.md`.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
