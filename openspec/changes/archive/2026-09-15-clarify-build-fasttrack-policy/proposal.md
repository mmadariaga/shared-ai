> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
Policy lines 42-43 read as if `/sai-build` never activates fast-track, while the meta-build coordinator always injects it for the chained apply with a supervisor banner. This wording-only change separates the explicit-token no-op from the always-injected chained apply fast-track with no behavior change.

## What Changes
- Reworks `sai/policies/fast-track-flag.md` build/review bullets: `/sai-review` keeps a pure no-op strip sentence, `/sai-build` gains a build-local no-op sentence plus always-injected `fast_track_active=true` session state and single supervisor banner semantics.
- Preserves presence-plus-strip first, invocation-scoped session state never written to a file, identical behavior with and without the explicit token, single banner once at apply activation, zero banners when apply never activates, and no second banner from the chained apply skipped shell.
- Makes no change to `sai/commands/meta-build/coordinator.md`, no banner literal change, and no behavior change.

## Capabilities
Single policy-clarification capability grounded in the staged diff.

### New Capabilities
- fast-track-policy-clarification: disambiguate explicit vs injected activation and banner ownership for `/sai-build`.

### Modified Capabilities
- None.

## Impact
- Modified: `sai/policies/fast-track-flag.md` — wording-only clarification, 11 insertions and 2 deletions.
- Unchanged: `sai/commands/meta-build/coordinator.md` — same unconditional injection, same banner literal, same activation scope.
- New backfill artifacts (coordinator-written): `openspec/changes/clarify-build-fasttrack-policy/.openspec.yaml`, `proposal.md`, `specs/fast-track-policy-clarification/spec.md`.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
