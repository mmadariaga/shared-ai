> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

The model-customization checklist now communicates relative reasoning demand for each target. The table previously showed target names and model settings but did not expose workload level.

## What Changes

- `bin/model-customization.js` adds a family-qualified task-complexity taxonomy and renders an aligned `TASK COMPLEXITY` column between `TARGET` and `SETTING` for both harness adapters.
- `test/model-customization-menu.test.js` covers the new column, alignment, complexity values, both harness flows, stable selections, and unavailable settings.
- `test/model-customization-taxonomy.test.js` covers family-qualified lookup and prevents bare-name collisions.

## Capabilities

The model-customization menu now exposes target workload levels without changing selection behavior.

### New Capabilities

None.

### Modified Capabilities

- **Model customization table:** displays `TASK COMPLEXITY` between target and setting columns with aligned headers, separators, and rows.
- **Complexity taxonomy:** maps worker, agent, command, and utility targets by family-qualified identity to arrow-based workload levels.

## Impact

- Modified `bin/model-customization.js`.
- Modified `test/model-customization-menu.test.js`.
- Modified `test/model-customization-taxonomy.test.js`.
- The existing `model-customization-menu` capability specification requires a subsequent delta-spec sync for the new column and taxonomy.
  Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
