> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

Project sources drifted from the Go+Zen preset (commands/agents on free#high, matrix on muse#high / glm-max); aligning restores the intended cost/quality routing. The preset is the single source of truth for opencode model and variant defaults.

## What Changes

- `agents/opencode/budget.md`, `agents/opencode/executor.md`, `agents/opencode/explore.md` — model variant `#high` to `#xhigh`, retaining `opencode/muse-spark-1.3-contributor-free`.
- `commands/opencode/sai-commit.md`, `sai-pr.md`, `sai-retire-docs.md`, `sai-status.md`, `sai-worktree.md` — model `opencode/muse-spark-1.3-contributor-free#high` to `opencode-go/muse-spark-1.3-contributor#xhigh`.
- `commands/opencode/sai-explore.md` — model `opencode-go/muse-spark-1.3-contributor-free#xhigh` to `opencode-go/muse-spark-1.3-contributor#xhigh`.
- `sai/install-manifest.json` — worker-matrix `opencodeAgent` entries aligned to preset: spec, design, implementation, backfill, merge, red, green, direct-build, and review-fix workers to `opencode-go/deepseek-v4.1-flash#max`; performance and accessibility workers from `opencode-go/glm-5.3-flash#max` to `opencode-go/deepseek-v4.1-flash#max`; commit worker to `opencode-go/muse-spark-1.3-contributor#xhigh`.
- `test/canonical-opencode-agent-behavior.test.js` — expected generic-agent model updated from `opencode/muse-spark-1.3-contributor-free#high` to `opencode/muse-spark-1.3-contributor-free#xhigh` to match the implemented agent frontmatter.
- Only `model:` lines in markdown and `model` plus `variantLine` in the manifest changed; all other frontmatter, descriptions, permission blocks, and JSON fields are intact. Already-matching entries were left untouched.

## Capabilities

### New Capabilities

- `opencode-defaults-alignment`: opencode default models and variants in versioned project sources match the Go+Zen preset in single-line `model#variant` form.

### Modified Capabilities

None — all behavior in this change is new.

## Impact

- Modified: `agents/opencode/budget.md`, `agents/opencode/executor.md`, `agents/opencode/explore.md`
- Modified: `commands/opencode/sai-commit.md`, `commands/opencode/sai-explore.md`, `commands/opencode/sai-pr.md`, `commands/opencode/sai-retire-docs.md`, `commands/opencode/sai-status.md`, `commands/opencode/sai-worktree.md`
- Modified: `sai/install-manifest.json`
- Modified: `test/canonical-opencode-agent-behavior.test.js`
- No new files were added.

Known limitations are the deliberate acceptance that installed `.opencode/` and user-global projections stay drifted until a separate reinstall, keeping this change review-sized to versioned project sources only.

Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
