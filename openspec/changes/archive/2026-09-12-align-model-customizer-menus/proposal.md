> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

The scope screen offered Workers second while the All view led with Agents then Orchestrator blocks, and the All rows ran together with right-aligned TYPE, hurting scanability.

## What Changes

- Reordered SCOPE_OPTIONS to All, Agents, Orchestrators, Workers, Utilities in bin/model-customization.js
- All checklist emits blank separator rows between agents, the orchestrator-worker middle block, and utilities with empty-group suppression; single-family scopes stay flat
- runNavigator in bin/install-flow.js treats blank rows as non-selectable with cursor skip, Space no-op, Enter exclusion, and empty-confirm protection, with parseTarget filtering
- Switched TYPE header and rows to left-aligned width 12 in bin/model-customization.js with TARGET, TASK COMPLEXITY, and SETTING unchanged and selection using selectable values
- Updated checklist seam assertions in test/model-customization-menu.test.js and scope order in test/model-customization-taxonomy.test.js

## Capabilities

### New Capabilities

- None — all changes modify existing capabilities.

### Modified Capabilities

- `model-customizer-scope`: scope order mirrors All view
- `model-customizer-grouping`: All grouping with blank separators and non-selectable rows
- `model-customizer-labels`: TYPE left-aligned at width 12

## Impact

- Modified bin/model-customization.js for scope order, All grouping, and TYPE alignment
- Modified bin/install-flow.js for non-selectable separator behavior in the shared navigator
- Modified test/model-customization-menu.test.js for separator-aware items, defaults, header, and displayOptions
- Modified test/model-customization-taxonomy.test.js for scope order
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
