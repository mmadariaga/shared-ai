> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

## Why

Model assignments across the SAI pipeline needed updating to reflect current provider availability and cost-performance trade-offs. Copilot commands migrated from Claude Sonnet 4.6 to GPT-5.4, the opencode design wrapper moved from kimi-k2.6 to glm-5.2, and two new opencode-only model-tier variants (`sai-3-implement-low`, `sai-3-implement-high`) were added to give users cheaper and higher-reasoning options for the implementation phase.

## What Changes

- **AGENTS.md**: Documented opencode-only `-low`/`-high` model variants in prerequisite check list, artifact table, and mirror discipline exception.
- **README.md**: Updated model routing table — Copilot explore/backfill switched to GPT-5.4, design switched to glm-5.2, added `implement-low` and `implement-high` rows with footnote. Replaced intelligence-vs-cost chart image.
- **commands/copilot/sai-backfill.prompt.md**: Model changed from `Claude Sonnet 4.6 (copilot)` to `GPT-5.4 (copilot)`.
- **commands/copilot/sai-explore.prompt.md**: Model changed from `Claude Sonnet 4.6 (copilot)` to `GPT-5.4 (copilot)`.
- **commands/opencode/sai-2-design.md**: Model changed from `opencode-go/kimi-k2.6` to `opencode-go/glm-5.2`.
- **commands/opencode/sai-3-implement-low.md**: New file — opencode-only wrapper using `opencode-go/minimax-m3`.
- **commands/opencode/sai-3-implement-high.md**: New file — opencode-only wrapper using `opencode-go/glm-5.2`.
- **skills/universal/sai-commands/SKILL.md**: Registry updated with `sai-3-implement-low` and `sai-3-implement-high` entries.
- **Intelligence-vs-Cost-(17-Jun-'26).png**: New chart image replacing `Intelligence-vs-Cost-(3-Jun-'26).png`.

## Capabilities

### New Capabilities

- `model-variant-wrappers`: Opencode-only model-tier variants (`-low`, `-high`) of `sai-3-implement`, allowing users to select cheaper or higher-reasoning models for the implementation phase without affecting other harnesses.

### Modified Capabilities

- `model-routing`: Updated model assignments across Copilot wrappers (explore, backfill → GPT-5.4), opencode design wrapper (→ glm-5.2), and command registry (new entries for variant wrappers).

## Impact

- **New files**: `commands/opencode/sai-3-implement-low.md`, `commands/opencode/sai-3-implement-high.md`, `Intelligence-vs-Cost-(17-Jun-'26).png`
- **Modified files**: `AGENTS.md`, `README.md`, `commands/copilot/sai-backfill.prompt.md`, `commands/copilot/sai-explore.prompt.md`, `commands/opencode/sai-2-design.md`, `skills/universal/sai-commands/SKILL.md`
- **Deleted files**: `Intelligence-vs-Cost-(3-Jun-'26).png`
- **Out of scope**: `design.md`, `tasks.md`, `implementation.md` — not generated by `/sai-backfill`
