> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

## Why

Minor documentation and model reference updates were needed to keep Copilot support information accurate: the README did not mention Copilot CLI as an unsupported surface, and the budget-subagent agent file referenced an outdated GPT-5 mini model identifier.

## What Changes

- `README.md`: Clarified that GitHub Copilot support excludes Copilot CLI in addition to the agents window (preview).
- `agents/copilot/budget-subagent.agent.md`: Updated model reference from `GPT-5 mini (copilot)` to `GPT-5.4 mini (copilot)`.

## Capabilities

### New Capabilities

`copilot-cli-support-clarification`: README now explicitly states Copilot CLI is not a supported surface for shared-ai.

### Modified Capabilities

`copilot-budget-subagent-model-update`: Budget subagent model reference updated to GPT-5.4 mini to match current Copilot model availability.

## Impact

- **New files**: none
- **Modified files**: `README.md`, `agents/copilot/budget-subagent.agent.md`
- **Out of scope**: `design.md`, `tasks.md`, `implementation.md` — not generated by `/sai-backfill`
