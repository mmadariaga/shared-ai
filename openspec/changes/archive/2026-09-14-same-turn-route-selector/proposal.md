> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
The block-plus-stop crystallization close forced one extra narration turn before explore-slice@1 idle could serve the route choice, which read from outside as a missing selector. Fusing block emission with the crystallization-close selector removes that roundtrip while staying route-neutral until Manual resolves.

## What Changes
- `sai/commands/explore/instructions.md`: shared crystallization-turn close now clears the stage TODO, emits the recordedList for the emitted set, and presents the crystallization-close route selector same-turn as the final emission, with no handoff or recommendation in the emission turn, panel empty until resolution with first render only at resolution, fast-track never auto-selecting and no overview-language question at emission, and inline-refusal, uncertainty POC pause, and store or panel failure unchanged with no fusion, while explore-slice@1 idle re-presents only while pending slices remain.
- `sai/commands/explore/steps/crystallization-protocol.md`: single-change emission now clears TODO, emits recordedList, and presents the selector same-turn with no handoff in the emission turn, and sliced emission now records names for same-turn choice, emits recordedList, presents the selector same-turn, and runs the existing deterministic choice with sole dispatching directly and multiple using ordered picker plus Cancel where Cancel dispatches nothing.
- `sai/commands/explore/steps/route-selector.md`: deferred choice renamed to crystallization-close choice with first presentation same-turn at close after the final separator with recordedList emit, re-presented by idle only while pending slices remain with no machine version bump, presented only through the native picker with three fixed titles and stable identities, fast-track never auto-selecting, and exclusions unchanged with no fusion.
- `sai/commands/explore/steps/slice.md`: first choice now runs same-turn at crystallization close with recordedList emit, and idle re-presents the choice only while pending slices remain with no version bump.
- Trade-off realized: a longer single emission turn in exchange for one fewer roundtrip, with Plan, Direct, and Manual terminal texts and worker scopes unchanged.

## Capabilities
### New Capabilities
- None

### Modified Capabilities
- `crystallization-close` — same-turn block plus recordedList plus selector close with no emission-turn handoff and empty panel until resolution.
- `deferred-selector` — same-turn first presentation at close with idle re-presentation while pending.
- `explore-pipeline-selector` — emission-turn route neutrality with same-turn selector, fast-track never auto-selecting, and preserved exclusion behavior.
- `explore-crystallization-block` — shared close includes recordedList and same-turn selector with no emission-turn handoff.
- `route-owned-todo` — same-turn choice preserves empty-panel-until-resolution with first render only at resolution.

## Impact
- Modified files: `sai/commands/explore/instructions.md`, `sai/commands/explore/steps/crystallization-protocol.md`, `sai/commands/explore/steps/route-selector.md`, `sai/commands/explore/steps/slice.md`
- New files: None
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
