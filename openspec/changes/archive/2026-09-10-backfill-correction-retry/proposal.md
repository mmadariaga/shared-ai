> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
A four-line draft naming defect failed a full Direct Build run with no in-frame repair, wasting the implemented diff and forcing manual copy-paste. The archive CLI already gates bad syncs, so pre-write rejections grounded in backfill drafts should converge through same-worker correction instead of closing the run. The staged diff (4 files, 21 insertions, 20 deletions) removes the one-shot, spent, and veto guards across the backfill worker contract and the Direct Build supervision path to enable that convergence.

## What Changes
- sai/commands/backfill/worker.md: replaces the one-shot and idempotency-protected execution paragraph with late-after-success rejection, partial-mutation no-refire reporting, and same-run correction feedback that recomposes only named sections from the staged diff, block grounding, and CLI error with no commit authorization.
- sai/commands/backfill/instructions.md: replaces the exactly-once execute resumption with resumable execute plus post-execution correction feedback, repeated-execute-after-success rejection, partial-mutation no-refire, and non-final repeated-defect and partial-mutation closures.
- sai/commands/archive/coordinator.md: replaces the one-shot no-retry stop with backfill-artifact error evaluation returning the verbatim error for same-worker backfill correction and archive relaunch, plus failed-retryable closure for repeated defects without progress, exact-state reporting with no refire after partial mutation, late-after-success rejection, and non-final closures.
- sai/commands/explore/steps/pipeline-direct-build.md: removes the one-shot qualifier from the commit-authorized flag and replaces the never-resend failure stops with partial-mutation no-refire, backfill-artifact error correction routing with archive relaunch, failed-retryable closure, late-after-success rejection, and non-final closures.
- No scope drift: every diff item maps to a stated intent item; no evidenced-but-unstated behavior was specified from the diff alone.

## Capabilities
### New Capabilities
None — all capabilities in this change modify existing capabilities.
### Modified Capabilities
- explore-pipeline-supervision: Direct Build failure handling gains evaluate-as-backfill-artifact-error, feedback-to-same-backfill-worker, and archive relaunch with failed-retryable, partial-mutation, late-after-success, and non-final closure rules.
- auto-fast-backfill-execution: backfill execution accepts same-worker correction feedback recomposing only named sections, retains late-after-success rejection and partial-mutation no-refire, and closes repeated defects without progress as failed-retryable.
- routed-archive-command: reconstruction drops the one-shot qualifier and the Direct Build continuation routes backfill-artifact CLI failures to same-worker correction with relaunch under the supervision contract.
- archive-content-recovery: the classified content-fix loop stays ordinary-route only while Direct Build backfill-artifact errors use backfill correction with relaunch instead of single-use close.
- auto-fast-archive-execution: archive CLI-failure stops are retained at the worker while supervision routes backfill-artifact errors to backfill correction before relaunch.

## Impact
Modified files:
- sai/commands/archive/coordinator.md (13 insertions, 7 deletions): execution-state rename and correction-plus-relaunch failure handling.
- sai/commands/backfill/instructions.md (2 insertions, 2 deletions): resumable execute and correction-feedback completion boundary.
- sai/commands/backfill/worker.md (3 insertions, 8 deletions): correctable execution continuation with named-section recomposition.
- sai/commands/explore/steps/pipeline-direct-build.md (3 insertions, 3 deletions): run-state flag rename and E3 failure routing for correction and relaunch.
New files: none in the implementation diff; the backfill drafts themselves are the change artifacts.
Known limitations carried from the block: a broken fixer can burn iterations until the repeated-defect stop fires with no round cap in this cut; post-mutation failures still end the run with manual /sai-archive exit and explicit user-request-only retries.
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
