> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: low (11 files, docs/prompts only, no runtime change)

## Why
Usage knowledge for the `sai-state` stage machine is duplicated and incomplete across prompts. `AGENTS.md` still describes the removed HTTP/token model, and Windows PowerShell quoting slips plus unclear errors cause avoidable `INVALID_EVENT` friction.

## What Changes
- Add `sai/policies/stage-machine.md` (121 lines) as the single operational source: local per-chat CLI store model, `spawn` / `emit` / `close` verbs, `machineId` handling, closed errors (`INVALID_EVENT`, `UNKNOWN_MACHINE`, `VERSION_MISMATCH`), Windows PowerShell quoting, `next.follow` loading, degraded-mode handling, operations-only boundary, and the canonical example living only there.
- Include the policy in every command mentioning `sai-state` (`sai/commands/explore/instructions.md`, `sai/commands/spec/coordinator.md`, `sai/commands/design/coordinator.md`, `sai/commands/explore/steps/pipeline-direct-build.md`, `sai/commands/explore/steps/pipeline-plan-unattended.md`, `sai/commands/explore/steps/pipeline-selector.md`) via `Fetch @sai/policies/stage-machine.md`; delete each duplicated operational block and keep only the owning command's event table.
- Remove the outdated `Explore stage machine (sai-state/)` section from `AGENTS.md` (HTTP/token model, 11 lines) and fix the `bin/sai-state.js` line to `spawn / emit / close`.
- Update stage-machine consumption tests to assert policy inclusion and no raw verb spellings in prompts.
- No runtime change, no shim, no `PATH` change, no per-machine event catalog in the common policy, no `GLOSSARY.md`, ADR/DDR, or archived-spec rewrites.

## Capabilities
### New Capabilities
- `stage-machine-common-policy`: single operational source plus the inclusion contract and `AGENTS.md` cleanup (docs and references only).

### Modified Capabilities
- None — no main-spec requirements modified; overlapping specs are carried as informational conflict context for archive sync.

## Impact
- New: `sai/policies/stage-machine.md`
- Modified: `AGENTS.md`, `sai/commands/design/coordinator.md`, `sai/commands/explore/instructions.md`, `sai/commands/explore/steps/pipeline-direct-build.md`, `sai/commands/explore/steps/pipeline-plan-unattended.md`, `sai/commands/explore/steps/pipeline-selector.md`, `sai/commands/spec/coordinator.md`, `test/explore-pipeline-selector.test.js`, `test/explore-stage-machine-consumption.test.js`, `test/explore-step-reachability.test.js`
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
