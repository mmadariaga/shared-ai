> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

Copy-paste from rendered views loses bold formatting and users who supplied complete change information still fell through to blind re-asks. The staged diff replaces the byte-exact heading-plus-bold-label gate with a minimal bold-insensitive plain-literal quorum so zero-ask pastes keep working while authorization rules stay token-gated.

## What Changes

Changed `sai/commands/backfill/instructions.md` (7 insertions, 7 deletions):
- Block-attempt detection searches the nine mandatory consulted plain literals (`Change name`, `What`, `Why`, `Capabilities in scope`, `Alternatives Considered`, `Trade-offs Accepted`, `Key constraints`, `Edge Cases`, `Implementation Details`) as case-sensitive exact-spacing substrings after stripping `**`, anywhere with no line-start anchor. The `## Ready to Propose` heading is not required and is ignored when present. Empty or `- None` counts as present. Prose matches count (false positives accepted). Extras ignored; `Request Additional Notes` never required nor counted.
- Every consumed field resolves by existence alone: literal exists implies skip that slot's question (carry value when content, still skip when empty/`- None` with no fallback); otherwise mine surrounding prose; otherwise restore the ordinary ask.
- Fixed-question mapping is existence-based: `Why` skips Q1; `Trade-offs Accepted` plus `Key constraints` together skip Q2. Name rule uses the bold-insensitive `Change name` literal with auto-accept only under resolved diff-source plus `--fast-track`. Authorization-gated suppressions (reconciliation, conflict auto-continue, name auto-accept) keep token rules.

## Capabilities

### New Capabilities

None — all changes modify existing backfill intake, reconciliation, interview, and fast-track behavior.

### Modified Capabilities

- backfill-unattended-intake: bold-insensitive plain-literal quorum replaces heading-plus-byte-exact gate; field resolution by existence.
- backfill-intent-reconciliation: block test without heading; consumed fields skip by existence identically with/without fast-track.
- backfill-interview: Q1/Q2 skip by literal existence (Why alone; Trade-offs Accepted plus Key constraints).
- backfill-fast-track: Change-name plain literal with token-gated direct acceptance preserved.

## Impact

Modified: `sai/commands/backfill/instructions.md` (only file in staged diff).

Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
