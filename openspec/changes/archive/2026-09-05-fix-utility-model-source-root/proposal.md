> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
`materializeLocalOverride` resolved the `utility` family to the global agents directory, causing a missing-source skip and leaving project-local utility overrides such as `.opencode/commands/sai-retire-docs.md` uncreated, so the model setting selected in `bin/install setup` never persisted or displayed.

## What Changes
- Correct `sourceRoot` in `bin/model-customization.js` so `family === 'utility'` reads from the global commands directory, matching the existing `familyDirectory` and `effectiveSetting` behavior.
- Leave enumeration (`UTILITY_NAMES`, `enumerateCommands`), `familyDirectory`, and `effectiveSetting` unchanged as the correctness reference.
- Add regression coverage for utility first-customization persistence on both harnesses, existing-override patching, missing-source skip, family source-root preservation, and adapter `createLocalOverride`.

## Capabilities
### New Capabilities
- None — this change introduces no new capability; all behavior modifies existing persistence.

### Modified Capabilities
- model-customization-menu — persist the selected model/variant for utility targets on first customization from the global commands directory.

## Impact
- Modified: `bin/model-customization.js` — one-line `sourceRoot` condition correction.
- Added: `test/model-customization-utility-source-root.test.js` — regression suite for all five utility targets, both harnesses, existing-override, decoy/absent-source, family roots, and adapter path.
- Verified against existing `model-customization` menu and taxonomy suites plus the setup flow with no regression; fix-loop converged with no findings.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
