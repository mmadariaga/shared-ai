> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
Users finishing Plan-Unattended received only a minimal `Next step: run /sai-build {name}.` line and missed where to review, where to run the build, and how to continue slices. The staged implementation expands the successful Plan-Unattended terminal into an ordered handoff contract: review artifacts here without touching code, run the build in another chat, and continue slices with a titled `next-slice`.

## What Changes
- `sai/commands/explore/steps/pipeline-plan-unattended.md`: successful conditional design terminal (both opted-in generation and no-generation branches) now prints the expanded successful close in review → build → next-slice order with `/sai-build`, `next-slice`, `{name}` kept verbatim English; success sentence and phase-aware autonomy-audit sentence updated to the same expanded close; failure, cancellation, continuation-failure, and STOP paths unchanged.
- `test/explore-pipeline-selector.test.js`: successful-Plan test now asserts the expanded close (`/sai-build {name}` in another chat plus `next-slice`) and retains the obsolete-text exclusion.

## Capabilities
### New Capabilities
- plan-terminal-close: expanded successful Plan-Unattended terminal covering review feedback here, build in a new chat, and titled next-slice guidance.
### Modified Capabilities
- None. Existing failure, cancellation, and STOP guidance is unchanged; the expansion adds terminal wording only.

## Impact
- Modified: `sai/commands/explore/steps/pipeline-plan-unattended.md`
- Modified: `test/explore-pipeline-selector.test.js`
Out-of-scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
