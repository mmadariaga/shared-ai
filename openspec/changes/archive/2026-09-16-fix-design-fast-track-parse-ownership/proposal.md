> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
The fast-track policy claimed coordinator-side parse for design while the design worker implements worker-side parse and returns the notice, the design coordinator disclaims parsing, and the live specs asserted both sides. Any future edit would break one spec, so the policy prose is aligned to worker-parse ownership with no behavior change.

## What Changes
- Retouches sai/policies/fast-track-flag.md canonical model item 1 to except sai-2-design from coordinator-owned parse and banner, allowing phase-owned parse exceptions for sai-2-design and sai-backfill.
- Adds a deliberate documented exception bullet for sai-2-design: worker owns parse-plus-strip on every route, routed and supervised, coordinator never parses; routed route returns a nonterminal notice carrying > FAST-TRACK MODE ACTIVE with dedup (fast_track_banner_emitted initialized and updated by the coordinator, carried in worker reconstruction fields); supervised route parses and strips with no notice and Explore owns the chained-segment banner; worker never prints the banner line directly.
- Keeps banner ownership and parse order unchanged: presence-plus-strip first, single visible confirmation per invocation.

## Capabilities
### New Capabilities
- None — docs-only alignment introduces no new capability.

### Modified Capabilities
- `sai-fast-track-flag` — corrects the opt-in membership clause and design scenario to design worker-parse ownership, keeping banner ownership and parse order unchanged.

## Impact
- Modified: sai/policies/fast-track-flag.md (12 insertions, 2 deletions, staged).
- Delta follow-through owned by backfill drafts: openspec/specs/sai-fast-track-flag/spec.md clause plus pinned scenario rewritten to worker-parse via ADDED plus REMOVED delta.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
