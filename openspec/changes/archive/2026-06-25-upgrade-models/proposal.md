> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

## Why

The opencode model assignments for `sai-1-spec` and `sai-backfill` were upgraded from `opencode-go/qwen3.7-plus` to `opencode-go/minimax-m3` to align with updated model recommendations for these commands.

## What Changes

- `commands/opencode/sai-1-spec.md`: frontmatter `model` changed from `opencode-go/qwen3.7-plus` to `opencode-go/minimax-m3`
- `commands/opencode/sai-backfill.md`: frontmatter `model` changed from `opencode-go/qwen3.7-plus` to `opencode-go/minimax-m3`
- `README.md`: model reference table updated for `spec (1)` and `backfill` rows to reflect `opencode-go/minimax-m3`

## Capabilities

### New Capabilities

None.

### Modified Capabilities

`model-routing`: The opencode model for `sai-1-spec` and `sai-backfill` commands is now `opencode-go/minimax-m3` instead of `opencode-go/qwen3.7-plus`.

## Impact

- **New files**: none
- **Modified files**: `README.md`, `commands/opencode/sai-1-spec.md`, `commands/opencode/sai-backfill.md`
- **Out of scope**: `design.md`, `tasks.md`, `implementation.md` — not generated by `/sai-backfill`
