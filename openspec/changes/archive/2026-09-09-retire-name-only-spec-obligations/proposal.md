> **⚠ POST-HOC RECORD** — This proposal was reconstructed after implementation from a crystallized `Ready to Propose` block. It describes a decision already made, not one being proposed.

**Complexity**: medium (S1 = 2 capabilities in 2-3; S2 = 2 requirements; S5 = 4 impact paths in 3-8; no breaking change, no new dependency)

## Why

`spec-require-block-input` retired the name-only creation path but left three surfaces still mandating it: that change's own proposal recorded `selector-copy` and `explore-crystallization-language-gate` as "informational conflict, no MODIFIED delta in this set", so the obligations were flagged and then never retired. The result is two active specs whose SHALL contradicts the governing requirement, and a live contract file that still lists the retired literal.

## What Changes

- `openspec/specs/selector-copy/spec.md` — the Manual description requirement and its scenario move from the name-suffixed `/sai-1-spec <change-name>` form to the block-paste form.
- `openspec/specs/explore-crystallization-language-gate/spec.md` — the preserved-English literal list drops the name-suffixed literal, retaining `review-loop`, `/sai-1-spec`, and `/sai-2-design`.
- `sai/commands/explore/steps/crystallization-language-gates.md` — the same literal list in the live contract file drops the retired entry.
- `sai/commands/explore/steps/pipeline-selector.md` — the Manual bullet's indentation is normalized from 8 to 6 spaces to match its sibling options; rendered text unchanged.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `selector-copy` — the Manual selector description obligation now requires the block-paste form.
- `explore-crystallization-language-gate` — the preserved-English literal list no longer includes the retired name-suffixed literal.

## Impact

- Modified files: `openspec/specs/selector-copy/spec.md`, `openspec/specs/explore-crystallization-language-gate/spec.md`, `sai/commands/explore/steps/crystallization-language-gates.md`, `sai/commands/explore/steps/pipeline-selector.md`
- No behavior change to any command: the implementation already used the block-paste form; this change retires the obligations that contradicted it.
- Out of scope: `openspec/specs/specs-approval-gate/spec.md`, where `/sai-1-spec <change-name>` appears as a WHEN trigger describing invocation rather than a mandated user-facing literal.
- Out of scope: design.md, tasks.md, implementation.md — not generated by post-hoc reconstruction.

## Proposal Research Documentation

Implementation evidence — staged diff over base `9f85e1ab6eacc726ceb693d3c54d589e61b3c362`:

- `sai/commands/explore/steps/crystallization-language-gates.md:9` — literal list reduced to the three surviving literals, read directly from the diff hunk.
- `sai/commands/explore/steps/pipeline-selector.md:24` — leading whitespace only; the bullet text is byte-identical, read directly from the diff hunk.
- `openspec/specs/selector-copy/spec.md:26,31` — requirement and scenario reworded to the block-paste form.
- `openspec/specs/explore-crystallization-language-gate/spec.md:9` — literal list reduced.

Existing contracts consulted:

- `openspec/specs/spec-require-block-input/spec.md` — the governing requirement, "Manual path instructs block paste", which pins the surviving literal list to exactly `review-loop`, `/sai-1-spec`, and `/sai-2-design`.
- `openspec/changes/archive/2026-09-06-spec-require-block-input/proposal.md` — records the two capabilities as known-but-deferred conflicts, establishing this change as the deferred half.
- `openspec/specs/specs-approval-gate/spec.md` — read; its occurrences are invocation triggers, not mandated literals, so no delta.
- `test/explore-pipeline-selector.test.js`, `test/canonical-opencode-agent-behavior.test.js`, `test/spec-coordinator-worker.test.js` — assert the surviving literal list and the block-paste copy; verified passing (152/152).

No external URL was consulted; every source is in-repository.

## Additional Notes

- **Verification requires `--test-concurrency=1`**: the suite reports false negatives when run in parallel, because the `install`/`doctor` family serializes on a single validation lock. The default `npm test` is not a reliable verdict for this repository.
- **Rejected: update the implementation to satisfy the stale specs** — it would revert `spec-require-block-input`.
- **Rejected: rely on the tests as the source of truth** — those tests were themselves stale across three changes before being corrected, so specs remain the audit surface.
- **Known limitation**: this change retires obligations only on the two capabilities named above. Any future reconstruction of `selector-copy` from history must carry the retirement forward rather than resurrecting the literal.
