> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

## Why

The `sai-backfill` skill file existed at `.claude/skills/sai-backfill/SKILL.md` — a local installation path, not a source path. The install script in `INSTALL.claude.md` was copying from that local path instead of from the canonical repo source at `skills/claude/sai-backfill/SKILL.md`. The misplaced file was unnecessary and the install instruction was incorrect.

## What Changes

- Deleted `.claude/skills/sai-backfill/SKILL.md` — removed misplaced local copy that was being used incorrectly as an install source.
- Updated `INSTALL.claude.md` bash section: `cp` source changed from `.claude/skills/sai-backfill/SKILL.md` to `skills/claude/sai-backfill/SKILL.md`.
- Updated `INSTALL.claude.md` PowerShell section: `Copy-Item` source changed from `.claude\skills\sai-backfill\SKILL.md` to `skills\claude\sai-backfill\SKILL.md`.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `sai-backfill-install-source`: Install script now copies from the canonical repo source `skills/claude/sai-backfill/SKILL.md` instead of the local `.claude/skills/` path.

## Impact

- **Modified files**: `INSTALL.claude.md`
- **Deleted files**: `.claude/skills/sai-backfill/SKILL.md`
- **Out of scope**: `design.md`, `tasks.md`, `implementation.md` — not generated by `/sai-backfill`
