> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
Maintainer-facing contracts disagreed on six of seven phases and model tables drifted from wrappers and the install-manifest matrix, misguiding future changes. The implemented docs-only slice aligns AGENTS.md and README to the wrapper-plus-matrix truth source and corrects exit contracts to the failed lifecycle wording without changing execution behavior.

## What Changes
Corrected AGENTS.md Build, Meta-review, Design, and Spec model/effort lines to opus medium and opencode-go/muse-spark-1.3-contributor xhigh with medium-effort coordinator and medium-effort worker for design. Corrected README design line and model table rows for explore, implement, apply, build, and sai-review to opus medium alignment. Replaced STOP-quoted missing-proposal wording in accessibility, performance, review, and security common steps with return failed and the exact plain-text literal. Updated the pinned design test literal to medium-effort coordinator and medium-effort design worker. No allowed-tools, binding load, or generated-artifact change.

## Capabilities
Two diff-backed capabilities, both new deltas.

### New Capabilities
- contract-clarity: accurate exit contracts for maintainers across four audit common steps.
- doc-accuracy: AGENTS.md and README match wrappers and install-manifest matrix plus pinned test literal.

### Modified Capabilities
No modified capabilities — all evidenced capabilities are new deltas.

## Impact
Modified files:
- AGENTS.md — Build, Meta-review, Design, Spec model/effort alignment
- README.md — design line and model table alignment
- sai/commands/accessibility/steps/common.md — exit literal alignment
- sai/commands/performance/steps/common.md — exit literal alignment
- sai/commands/review/steps/common.md — exit literal alignment
- sai/commands/security/steps/common.md — exit literal alignment
- test/design-coordinator-worker.test.js — pinned literal update to medium-effort coordinator and medium-effort design worker
Known limitations carried from Q2: test literal churn in the same change; narrow scope defers binding and orphan-file cleanup to later slices; fix-loop round 1 converged with no findings; design test 109 pass with 2 pre-existing fails identical on base; review P0 Pass 11 versus 12 pre-existing out of scope.
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
