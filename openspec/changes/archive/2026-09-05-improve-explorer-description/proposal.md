**Complexity**: low (2 files, description-only)

> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

Main agents bypassed the research subagent with direct reads and missed index-aware currency judgment and context hygiene. A sharper outcome-focused frontmatter description improves discovery and delegation for SAI-built projects.

## What Changes

- `agents/opencode/explore.md`: frontmatter `description` now sells fast, cost-effective read-only exploration for SAI-built projects, noting it knows their architecture and where to start versus generic search, with bounded summaries and no writes.
- `agents/claude/budget-explorer.md`: frontmatter `description` now sells the same outcome for the Claude Code `budget-explorer` agent, with the same architecture and starting-point framing, bounded summaries and no writes.
- Preserved in both files: `model`, `mode`/`effort`, `tools`, and body Fetch directives unchanged; description stays in concise English without enumerating codegraph or index internals.
- No caller-guidance, policy, skill, `remember.md`, or README changes in this slice.

## Capabilities

### New Capabilities

- `explorer-discovery`: sharper subagent selection via an outcome-focused description mirrored across both harnesses.

### Modified Capabilities

- None.

## Impact

- Modified: `agents/opencode/explore.md` — frontmatter `description` rewritten, all other frontmatter and body preserved.
- Modified: `agents/claude/budget-explorer.md` — frontmatter `description` rewritten, all other frontmatter and body preserved.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill

## Proposal Research Documentation

**Local files**: agents/opencode/explore.md, agents/claude/budget-explorer.md, sai/policies/explore-agent.md

**External URLs**: None

## Additional Notes

Description-only mirror across both harnesses; English and concise to avoid truncation in listings; no model, mode, tools, or body changes.
