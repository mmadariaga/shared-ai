> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: low (4 files, docs-only dedup)

## Why

The explore wrapper, shared bootstrap, and explore body disagreed on who preloads the spec-worker binding, and the explore bootstrap lacked the continuation sentence other bootstraps carry, so readers had to infer where execution continues.

## What Changes

- Removes the duplicate spec-worker binding preload from `commands/claude/sai-explore.md` and `commands/opencode/sai-explore.md`; the explore bootstrap remains the single owner.
- Rewords `sai/commands/explore/instructions.md` step 4 to scope the Boot preload claim to step files plus the stage machine, with worker bindings owned by the wrapper and bootstrap.
- Appends the continuation sentence to `sai/commands/explore/command-bootstrap.md` so the handoff to the harness-selected card is explicit.
- No runtime dispatch order change; documentation and preload dedup only.

## Capabilities

### New Capabilities

- `explore-preload` — single-owner preload documentation and bootstrap continuation for explore.

### Modified Capabilities

- None — docs-only clarification inside the new capability; no existing capability behavior changes.

## Impact

- Modified: `commands/claude/sai-explore.md` (remove one Fetch line)
- Modified: `commands/opencode/sai-explore.md` (remove one Fetch line)
- Modified: `sai/commands/explore/command-bootstrap.md` (append continuation sentence)
- Modified: `sai/commands/explore/instructions.md` (scope step-4 preload wording)
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
