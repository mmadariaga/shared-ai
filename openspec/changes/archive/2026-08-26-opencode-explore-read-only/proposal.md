> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

The managed opencode `explore` agent previously relied on fetched instructions to enforce read-only behavior. Its frontmatter now adds tool-level controls that deny direct `write` and `edit` while leaving `bash` available for research commands. This makes the intended direct-tool boundary explicit without changing the existing research route.

## What Changes

- `agents/opencode/explore.md` declares `tools.write: false`, `tools.edit: false`, and `tools.bash: true` while retaining its existing model, mode, and fetch directives.
- `test/canonical-opencode-agent-behavior.test.js` asserts the tool fields exactly once for `explore` and preserves the exact post-frontmatter wrapper body contract.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- **Managed opencode explore tool boundary** — The delegated `explore` agent denies direct `write` and `edit` tools, retains `bash` access, and preserves its fetch wrapper.

## Impact

- `agents/opencode/explore.md`: adds explicit per-agent tool restrictions.
- `test/canonical-opencode-agent-behavior.test.js`: adds regression assertions for the tool restrictions.
- The primary `sai-explore` route, writer workers, and Claude surfaces remain unchanged.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill.
