> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

## Why

The update aligns command metadata with current model naming and removes redundant wrapper metadata so command definitions stay consistent and easier to maintain.

## What Changes

- Updated `README.md` apply phase Copilot model label from `GPT-5 mini (copilot)` / `github-copilot/gpt-5-mini` to `GPT-5.4 mini (copilot)` / `github-copilot/gpt-5.4-mini`.
- Updated `commands/copilot/sai-4-apply.prompt.md` model from `GPT-5 mini (copilot)` to `GPT-5.4 mini (copilot)`.
- Removed `variant: default` from `commands/opencode/sai-4-apply.md` frontmatter.
- Removed `variant: default` from `commands/opencode/sai-pr.md` frontmatter.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `copilot-apply-model-selection`: Apply phase Copilot metadata now points to GPT-5.4 mini identifiers.
- `opencode-wrapper-frontmatter-defaulting`: Opencode wrappers now rely on implicit default variant behavior instead of declaring `variant: default` explicitly.

## Impact

- **New files**: None
- **Modified files**: `README.md`, `commands/copilot/sai-4-apply.prompt.md`, `commands/opencode/sai-4-apply.md`, `commands/opencode/sai-pr.md`
- **Out of scope**: `design.md`, `tasks.md`, `implementation.md` — not generated by `/sai-backfill`
