> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

**Complexity**: medium (3 capabilities, spec-only corrections, no runtime change)

## Why
Main specs for the stage-machine store claim behaviors the implementation does not provide: eventId replay idempotency, a `/restore` verb, a Node >= 22 startup gate with single-binary distribution, and a three-verb CLI heading that contradicts its four-verb body. Archive sync would propagate the drift.

## What Changes
- Correct `explore-stage-machine` requirements to match `bin/sai-state.js`: no eventId replay (empty eventId at persist), no `/restore` verb, ledger persisted but not compared.
- Correct `stage-machine-cli-interface` requirements to match the implemented CLI: four verbs `spawn` / `emit` / `reset` / `close`, deterministic UUID derivation, atomic session-file writes, ledger fields persisted without replay enforcement.
- Correct `session-state-lifecycle` runtime baseline to match the implemented script: Node stdlib-only with no version gate and no single-binary packaging.
- No runtime code change, no new behavior, no migration.

## Capabilities
### New Capabilities
- None — no new behavior introduced; all entries correct existing specs to match implementation.

### Modified Capabilities
- `explore-stage-machine`: remove replay and restore claims, clarify ledger-without-replay.
- `stage-machine-cli-interface`: fix verb count to four, clarify version/idempotency ledger semantics.
- `session-state-lifecycle`: align runtime baseline with stdlib-only script reality.

## Impact
- Modified: `openspec/specs/explore-stage-machine/spec.md`, `openspec/specs/stage-machine-cli-interface/spec.md`, `openspec/specs/session-state-lifecycle/spec.md` (via archive sync of this change's deltas)
- New: `openspec/changes/fix-stage-machine-spec-drift/.openspec.yaml`, `proposal.md`, `specs/explore-stage-machine/spec.md`, `specs/stage-machine-cli-interface/spec.md`, `specs/session-state-lifecycle/spec.md`
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
