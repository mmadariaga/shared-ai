> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

The active sai-explore contracts used mixed crystallization selector vocabulary, making it unclear which option represented planning versus direct implementation. The implemented update standardizes the visible build option as **Build - Unattended**.

## What Changes

The shared crystallization-close selector contract now uses **Build - Unattended** instead of **Direct build - Unattended** in its visible title and active references. The language-gate contract, question-context exemption, and selector contract tests use the same canonical title. The selector remains a three-option English-titled surface with localized descriptions.

## Capabilities

### New Capabilities

No new capability was introduced.

### Modified Capabilities

- `selector-label-clarity`: the crystallization-close selector presents clear fixed titles for its three execution modes.
- `selector-contract-consistency`: active selector references and contract assertions use the canonical build title.
- `harness-parity`: the shared selector presentation contract keeps the same titles and order across Claude Code and opencode.

## Impact

- Modified `sai/commands/explore/steps/crystallization-language-gates.md`.
- Modified `sai/commands/explore/steps/pipeline-selector.md`.
- Modified `sai/policies/question-context.md`.
- Modified `test/explore-contract-preservation.test.js`.
- Modified `test/explore-pipeline-selector.test.js`.

Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill.
