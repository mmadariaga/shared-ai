> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

The crystallization-close selector wording understated the pre-implementation human review pause and blurred Direct Build-Unattended against /sai-build, risking wrong route selection. The staged copy change revises the three one-line descriptions to emphasize human review, simple fixes, and full manual control.

## What Changes

- `sai/commands/explore/steps/pipeline-selector.md:22-24` — revised only the three one-line selector descriptions, preserving fixed English titles, fixed order, and route identities `plan-unattended`, `direct-build-unattended`, `manual`.
- Plan-Unattended description now states it runs `sai-1` and `sai-2` to create the plan and stops for pre-implementation human review.
- Direct Build-Unattended description now states it implements the change directly and updates specs afterward as ideal for fixes and simple changes, dropping the `it is not /sai-build` disambiguator per user confirmation with the accepted confusion risk.
- Manual description now directs the user to proceed manually with `/sai-1-spec <change-name>` with full control over the process.
- Known limitation carried from the fixed interview: removing the explicit /sai-build disambiguator trades clarity for brevity; accepted per user confirmation.

## Capabilities

### New Capabilities

- `selector-copy`: crystallization-close selector one-line descriptions SHALL emphasize pre-implementation human review for Plan-Unattended, direct implementation with post-hoc spec update for fixes and simple changes for Direct Build-Unattended, and full-control manual continuation with the verbatim `/sai-1-spec <change-name>` literal for Manual, while preserving titles, order, route identities, and verbatim literals.

### Modified Capabilities

- None.

## Impact

- Modified file: `sai/commands/explore/steps/pipeline-selector.md` (lines 22-24 descriptions revised; titles, order, route identities, and verbatim literals `sai-1`, `sai-2`, `/sai-1-spec <change-name>` preserved; shared file under `sai/` covers both harnesses via manifest projection).
- Retroactive records returned by this prepare run: `openspec/changes/update-crystallization-selector-copy/.openspec.yaml`, `proposal.md`, `specs/selector-copy/spec.md` (not yet written).
- Untouched by design: `openspec/changes/archive/` and any ADR/DDR index (no record for a copy tweak).
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
