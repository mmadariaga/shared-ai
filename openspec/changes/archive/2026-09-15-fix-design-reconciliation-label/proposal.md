> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: low (1 file, doc-only text fix, no behavior change)

## Why

The design coordinator text mislabeled reconciliation behavior and caused confusion at the table. sai/commands/design/coordinator.md:51 states pre-gate completed never reconciles, but the row labeled completed (pre-gate) ordered reconciliation on the unopted column, the invariants at :63 and sai/policies/todo-structure.md:40 govern only via cross-reading, and step 5 omitted reconciliation wording.

## What Changes

- Correct the reconciliation matrix in sai/commands/design/coordinator.md: pre-gate completed rows now state leave list as-is on both columns; post-gate rows now reconcile explicitly.
- Clarify the absent-token Continue route to state it reconciles all unmarked steps in the unopted plan.
- Keep invariants at :63 governing over the matrix; do not alter progress plans, pointer map, or step machine wiring.
- No behavior or routing change; doc-only text fix.

## Capabilities

### New Capabilities

- `design-coordinator-text`: fix reconciliation table labels and step 5 wording with no behavior change.

### Modified Capabilities

None.

## Impact

- Modified: `sai/commands/design/coordinator.md` (3 insertions, 3 deletions — reconciliation rows plus absent-token Continue wording).
- No new runtime behavior; no progress plans, pointer map, or step machine wiring altered.
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill

## Proposal Research Documentation

**Local files**:
- `sai/commands/design/coordinator.md:51-73`
- `sai/policies/todo-structure.md:40`
- `sai/commands/design/phase-contract.md:42-60`
- `openspec/specs/design-coordinator/spec.md`
- `openspec/specs/coordinator-progress-ownership/spec.md`
- `openspec/specs/review-step-evidence-marking/spec.md`

**External URLs**: None.

## Additional Notes

Doc-only text fix with no behavior or routing change. Staged implementation is 3 insertions and 3 deletions in sai/commands/design/coordinator.md. No progress plans, pointer map, or step machine wiring altered.
