> **POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
Agents can forget the read-only boundary during multi-step exploration and may attempt implementation work too early. A repeated instruction reduces that risk without changing route authorization.

## What Changes
- Added a read-only reminder paragraph near the beginning of sai/commands/explore/steps/common.md stating that sai-explore must not create, modify, or delete files or implement code, that emitting a Ready to Propose block does not authorize writes, that delegated writes require an explicitly selected route and that route's contract, and that the normative no-file-writes rule remains in sai/commands/explore/instructions.md.
- Kept the normative restriction in sai/commands/explore/instructions.md unchanged as the authoritative enforcement surface.
- Kept the state-machine hint technical and unchanged; no read-only rule was added to the hint.
- Left production code and OpenSpec artifacts unchanged; known debt retained is duplication of the read-only rule across two instruction surfaces to improve retention.

## Capabilities
### New Capabilities
- explore-read-only-discipline: shared exploration-stage reminder that exploration is read-only, crystallization is proposal emission only, and writes require an explicitly selected route.

### Modified Capabilities
- None.

## Impact
- Modified: sai/commands/explore/steps/common.md (2 insertions, reminder paragraph only).
- New files from implementation: none; instruction-only change with no production code change.
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
