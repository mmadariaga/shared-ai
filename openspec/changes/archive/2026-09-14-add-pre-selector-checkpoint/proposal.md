> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

Crystallization close could present the route selector before the proposal, task, and record state were consistently established. The implemented gate makes the close order explicit so the selector appears only after the required close state holds.

## What Changes

- Modified `sai/commands/explore/instructions.md` shared crystallization-turn close to require an explicit blocking CHECKPOINT before the route selector.
- Checkpoint enforces fixed order: visible `Ready to Propose` block(s) emitted, stage TODO cleared, `recordedList` emitted, then selector per `route-selector.md`.
- Single shared checkpoint runs once after the last `---` for single and sliced paths; `inline-refusal` path is excluded.
- Store or panel failure never skips the checkpoint; degraded mode applies and the selector is not presented blind; `fast-track` neither auto-selects nor skips the checkpoint.
- Staged diff: 1 file changed, 1 insertion, 1 deletion; no new files.

## Capabilities

### New Capabilities

- None

### Modified Capabilities

- `crystallization-close`: Adds ordered pre-selector checkpoint to the shared close, including single-checkpoint scope and preserved exclusion and failure behavior.

## Impact

- Modified: `sai/commands/explore/instructions.md` (shared close paragraph, 1 insertion, 1 deletion).
- No new files created; no other files modified.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
