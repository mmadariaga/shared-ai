> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why
The ready literal in `sai/tools/research-tools-check.js` carried a warning marker even when index, binary, and MCP presence were all true. A warning on the healthy path confuses scrollback and dilutes real fallback warnings.

## What Changes
- Update `sai/tools/research-tools-check.js` `LITERALS.ready` from `> ⚠️ **CodeGraph ready — structural research will use codegraph instead of grep/glob/Read.**` to `> **CodeGraph ready — structural research will use codegraph instead of grep/glob/Read.**`, keeping blockquote and bold emphasis while removing the warning marker.
- No change to detection logic in `classify()`, `LITERALS` keys, `status`, `ok:true`, exit codes, JSON shape, or verbatim-relay behavior.

## Capabilities
### New Capabilities
None.
### Modified Capabilities
- `explore-codegraph-fallback-notice` (ready-notice-tone scope): ready notice renders as blockquote and bold without `⚠️`, overlapping the visual-emphasis requirement.

## Impact
- Modified: `sai/tools/research-tools-check.js` — `LITERALS.ready` literal only; `classify()`, keys, `status`, `ok:true`, exit codes, and JSON shape unchanged.
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
