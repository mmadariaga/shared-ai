> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

The suite fails because the test still pins Pass 11 while the live review worker contract uses Pass 12, and the build composition cannot run coordinator-owned git and checklist work under its narrow toolset. This backfill records the already-implemented fix.

## What Changes

- Update test/review-coordinator-worker.test.js to pin Pass 12 instead of Pass 11, keeping the rest of the pattern.
- Widen commands/claude/sai-build.md allowed-tools to exact parity with commands/claude/sai-4-apply.md for the chained apply segment.
- Add test/build-coordinator.test.js coverage pinning the wide sai-build execution set to prevent copy-paste regression.
- Leave commands/opencode/sai-build.md unchanged since it declares no allowed-tools; the non-mirror is intentional.
- Leave all other narrow wrapper sets and their pinned tests untouched; sai-build stays out of those lists.

## Capabilities

### New Capabilities

- `review-test-correctness`: align the pinned Pass reference in the review coordinator-worker test with the live Pass 12 contract.
- `build-composition-execution`: run the chained apply segment under /sai-build on Claude Code with the apply-parity execution toolset.

### Modified Capabilities

- None — no existing capability behavior is modified; overlapping main specs travel as conflict context for archive delta-sync.

## Impact

- Modified commands/claude/sai-build.md — narrow node-scoped allowlist replaced with the wide apply-parity execution set.
- Modified test/review-coordinator-worker.test.js — Pass 11 expectation updated to Pass 12.
- Modified test/build-coordinator.test.js — new test asserting sai-build keeps exact apply parity and pins the wide set.
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
