> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: high (nine modified capabilities, 20 affected paths, no runtime code)

## Why

It removes a retained but inactive invocation entry contract that no supported direct or composed implementation route loads. Keeping it creates misleading source-layout and projection inventory, so active references and managed copies can drift from the coordinator → worker → step-gated route.

## What Changes

- Deletes the inactive `sai/commands/implement/invocation.md` surface.
- Removes its active references from fixtures, source-layout inventories, implementation tests, and maintained documentation.
- Registers `commands/implement/invocation.md` as a hash-gated retirement for both Claude Code and opencode so existing managed copies can be cleaned safely without recreating the projection.
- Keeps `sai/commands/implement/worker.md`, `steps/common.md`, and coordinator-selected step files as the active implementation route.
- Preserves direct `/sai-3-implement` behavior and the `/sai-build` implementation segment; the build command-bootstrap and coordinator surfaces are unchanged.
- Leaves historical OpenSpec changes and ADR references intact.
- Leaves the separate explore Auto-fast ordering fix outside this retirement slice.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `orchestration-source-layout`
- `implementation-harness-bindings`
- `implementation-behavioral-parity`
- `implement-steps-library`
- `invocation-core-provenance`
- `routed-contract-truth`
- `source-layout`
- `verified-precondition-handback`
- `harness-coordination-parity`

## Impact

- Modified `AGENTS.md` and `README.md` to describe retired compatibility assets and the active implementation step route.
- Modified `bin/orchestration-source-audit.js` to classify `sai/commands/implement/invocation.md` as retired.
- Modified `fixtures/implementation-adapter.js` and `fixtures/implementation-completion-step-4.js` to remove the retired invocation entries.
- Modified `sai/commands/implement/instructions.md` to retain its apply-only compatibility role.
- Deleted `sai/commands/implement/invocation.md`.
- Modified `sai/install-manifest.json` with the two-harness retirement record and four managed hashes.
- Modified the implementation, installer, source-layout, retirement, uninstall, and verified-precondition test inventories.
- Direct `/sai-3-implement` behavior remains routed through the coordinator, worker, and step library.
- `/sai-build` behavior remains unchanged; the focused build coordinator suite passes 7/7.
- Focused Slice 2 suites pass 68/68. The full suite retains two unrelated design-worker failures.
- Historical OpenSpec changes and ADRs remain outside the active-reference rewrite.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
