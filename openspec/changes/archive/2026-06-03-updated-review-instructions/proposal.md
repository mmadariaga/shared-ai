> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation. It describes a decision already made, not one being proposed.

## Why

The audit and review instruction files referenced a single `spec.md` artifact — a concept from an older version of the workflow. The current sai-workflow uses multiple artifacts (`proposal.md`, `design.md`, `specs/**/*.md`). All stale references needed to be replaced with explicit, accurate pointers to the current artifact structure so that agents loading those instructions operate against the right files.

## What Changes

- `sai/instructions/review.md` — replaced `spec.md` references with explicit multi-artifact references; renamed "Required Inputs" → "Prerequisites"; updated error message, report header (`**Spec:**` → `**Change:**`), domain-alignment check, findings template, and all inline prose
- `sai/instructions/security.md` — same pattern as review.md; updated "Acknowledged Trade-offs (from spec.md)" section header and template
- `sai/instructions/performance.md` — same pattern; updated report header, findings template, and acknowledged trade-offs section
- `sai/instructions/accessibility.md` — same pattern; updated report header, findings template, and acknowledged trade-offs section
- `sai/instructions/pr.md` — replaced `spec.md` and `implement.md` aliases with explicit file lists; renamed "Required Inputs" → "Prerequisites"; updated error message
- `sai/instructions/design.md` — updated cost discipline reminder to list `specs/**/*.md` alongside `proposal.md` as free-read files

## Capabilities

### Modified Capabilities

- `artifact-reference-model`: Audit and review instructions now reference `proposal.md`, `design.md` (if present), and `specs/**/*.md` as separate, named artifacts instead of treating them collectively as a single `spec.md`.

## Impact

- **New files**: none
- **Modified files**: `sai/instructions/review.md`, `sai/instructions/security.md`, `sai/instructions/performance.md`, `sai/instructions/accessibility.md`, `sai/instructions/pr.md`, `sai/instructions/design.md`
- **Out of scope**: `design.md`, `tasks.md`, `implementation.md` — not generated by `/sai-backfill`
