> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: medium

## Why
The main-session CodeGraph-ready literal pushed the main agent toward direct structural calls. Delegation is cheaper and keeps principal context clean, and the explorer already owned tool choice via its ladder plus per-segment discards, so all discovery I/O now goes to the explore subagent while the main session reasons and synthesizes.

## What Changes
Removed the main-session Research-tooling check block from `sai/commands/explore/body.md` (probe run, `--mcp-present` computation, literal relay) and replaced it with one normative Delegated research line plus load order. Deleted item 2 verbatim literal reprint from `sai/commands/explore/instructions.md` and renumbered items 3-11 to 2-10. Added Delegated research and Explorer-owned availability sections to `sai/policies/explore-agent.md` (delegation as efficient path, direct-read scope, punctual exceptions, explorer self-detection, discards as only signal, unavailable-explorer fallback, harness parity). Trimmed `commands/claude/sai-explore.md` allowlist: removed `mcp__codegraph__codegraph_explore` and collapsed per-file `Bash(node ...)` entries to per-root wildcards, keeping Read, Glob, Grep, WebFetch, WebSearch under written discipline. Left `sai/tools/research-tools-check.js` untouched; only unwired its invocation from explore. Updated six existing specs via MODIFIED deltas to retire the main-session probe, literals, and caller-supplied MCP flag.

## Capabilities
### New Capabilities
- explore-research-delegation: all discovery research in sai-explore delegated to explore with goal plus output contract; principal reasons and synthesizes and never researches code directly.
- explorer-owned-availability: CodeGraph availability self-detection lives only in the explorer; per-segment ladder_discards is the only signal.
### Modified Capabilities
- explore-codegraph-fallback-notice: main-session one-time check, three-state literals, English-only and emphasis rules retired; no main-session notice.
- explore-research-tooling-probe: probe unwired from explore path; caller-supplied --mcp-present removed from explore; detection explorer-owned.
- explore-instruction-delivery: literal-print rule and body.md probe ownership removed; delegation line is the contract.
- per-command-tool-scoping: sai-explore allowlist trimmed (CodeGraph MCP dropped, Bash collapsed to per-root wildcards).
- tool-preference-ladder: delegation mandated; explorer-only self-detection; discards as sole principal signal.
- ladder-discard-logging: per-segment ladder_discards promoted to only availability signal for the principal.

## Impact
Modified: `commands/claude/sai-explore.md` (allowlist trim and Bash wildcard collapse).
Modified: `sai/commands/explore/body.md` (probe block replaced by delegation line).
Modified: `sai/commands/explore/instructions.md` (literal-print item removed, renumbered).
Modified: `sai/policies/explore-agent.md` (delegated-research and availability sections added).
Preserved untouched: `sai/tools/research-tools-check.js` (invocation unwired only).
Spec deltas: 2 new capabilities (ADDED) plus 6 modified capabilities (MODIFIED) retiring the probe and literals.
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
