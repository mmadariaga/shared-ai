**Complexity**: medium

## Why

> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

The implementation worker's ADR/DDR culture check did not define a precise signal, allowing inconsistent behavior across the active implementation step, apply compatibility content, repository guidance, and installed harness projections.

## What Changes

Implementation planning now resolves the record family before checking culture and uses only the resolved family's physical `0000-INDEX.md` as the culture signal. An existing family index enables direct creation and warm-splice maintenance. A missing index requires explicit approval before creation and preserves the cold-build path. Decisions that fail any ADR/DDR criterion create no record and prompt no approval. The active step, compatibility guidance, repository guidance, and installation projections are covered by parity tests.

The selected diff also documents the physical-index rule in `AGENTS.md`. This is implemented documentation behavior evidenced by the diff.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `adr-creation-decision`: creation now depends on the resolved family's physical index, with no-op behavior for non-qualifying decisions and no ADR-versus-DDR choice.
- `adr-index-maintenance`: the existing index determines warm-splice versus approval-gated cold-build maintenance after creation.
- `implementation-behavioral-parity`: active, compatibility, repository, and harness-projected guidance remains aligned.

## Impact

- `AGENTS.md` — documents the resolved-family physical-index culture rule.
- `sai/commands/implement/steps/artifact-analysis.md` — updates the active Step 3 culture gate.
- `sai/commands/implement/instructions.md` — keeps apply compatibility guidance aligned.
- `test/adr-creation-culture-gate.test.js` — verifies index-present, index-absent, existing-record, empty, non-qualifying, family-resolution, and harness-parity cases.
- `openspec/changes/fix-adr-creation-culture-gate/specs/adr-creation-decision/spec.md` — records the creation-gate delta.
- `openspec/changes/fix-adr-creation-culture-gate/specs/adr-index-maintenance/spec.md` — records branch-selection preservation.
- `openspec/changes/fix-adr-creation-culture-gate/specs/implementation-behavioral-parity/spec.md` — records source and projection parity.
- The absent-index approval path and duplicated apply compatibility copy remain deliberate maintenance constraints.

Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
