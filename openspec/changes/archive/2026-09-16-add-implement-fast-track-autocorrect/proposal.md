> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

Implement stops on interface-contract gaps today even when a single fix preserves the proposal objective, stalling planning on defective sai-2 input. Fast-track should correct defective sai-2 artifacts and continue to avoid stalls, while sai-1 defects always escalate for human scope protection.

## What Changes

- `sai/commands/implement/coordinator.md` — owns --fast-track parse plus strip, emits > FAST-TRACK MODE ACTIVE once, propagates fast_track_active as invocation-scoped session state alongside the envelope, and carries it in replacement reconstruction fields.
- `sai/commands/implement/steps/plan-generation.md` — replaces unconditional STOP on unsatisfiable signatures with defective sai-2 handling: without fast-track STOPs as today, with fast-track auto-corrects only a single preserving sai-2 fix as amend then regenerates implementation.md.
- `sai/commands/implement/steps/validation.md` — adds fast-track auto-correction limits: never corrects sai-1, never auto-picks among ambiguous options, escalates when no path exists, never writes fast_track_active, never corrects change-overview.md directly.
- `sai/commands/implement/worker.md` — honors coordinator-declared fast_track_active only in plan-generation and validation branches, records every auto-correction in summary and implementation.md note, never parses the flag or emits the banner.
- `sai/commands/meta-build/coordinator.md` — injects fast_track_active true for both implement and apply segments, prints the single supervisor banner once at implement activation with no second banner for apply.
- `sai/policies/fast-track-flag.md` — adds sai-3-implement to canonical membership and updates build injection from apply-only to implement plus apply with single banner at first activation.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `sai-fast-track-flag`: adds sai-3-implement as parser member with coordinator-owned parse plus banner and session-state propagation, and updates composition banner plus state rules to implement plus apply.
- `fast-track-policy-clarification`: updates chained injection from apply-only to implement plus apply with single supervisor banner at first activation.
- `implement-respects-interfaces`: changes unsatisfiable-signature STOP into fast-track-gated defective sai-2 handling with single-preserving auto-correct continuing as amend plus audit boundary.

## Impact

**Modified production files**:
- `sai/commands/implement/coordinator.md`
- `sai/commands/implement/steps/plan-generation.md`
- `sai/commands/implement/steps/validation.md`
- `sai/commands/implement/worker.md`
- `sai/commands/meta-build/coordinator.md`
- `sai/policies/fast-track-flag.md`

**Main specs receiving delta updates at archive sync**:
- `openspec/specs/sai-fast-track-flag/spec.md`
- `openspec/specs/fast-track-policy-clarification/spec.md`
- `openspec/specs/implement-respects-interfaces/spec.md`

Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
