> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

## Why

ADR 0172d chose sibling worker dispatch for the fast lane over one long-lived mega-subagent. It cited two reasons: nesting pushes delegation depth past two, where an un-pre-approved permission prompt cannot render and hangs the session on opencode (anomalyco/opencode#13715), and nesting forces interview `needs_input` payloads to bubble through an intermediate worker, corrupting the opaque input history and verbatim-forwarding contracts.

The depth and `needs_input` hangs are fixed in opencode v2, so the hang justification is obsolete (docs/adr/0172d-sibling-worker-dispatch-for-the-fast-lane.md:13). The sibling decision itself still stands on its remaining ground: independent failure under Bounded Recovery and context hygiene across phases with different lifecycles. Users still on opencode v1 stay exposed to the hang if nesting at depth >= 2, so a v1 compatibility note is carried after the fix.

## What Changes

This change amends ADR 0172d without rewriting it and removes the obsolete hang sentence from the opencode fetch skill:

- New ADR 0185 amends 0172d: the #13715 hang justification is retired; the sibling topology is kept for recovery isolation and context hygiene; the v1/v2 note is kept (fixed in v2, v1 users nesting at depth >= 2 remain exposed).
- ADR index records the Amend relationship (command-group entry plus relation-table row) and keeps 0172d in force outside history.
- The opencode fetch skill keeps the path-composition rule and its observed dropped-segment evidence as prompt hygiene; only the hang sentence (permission prompt never rendering at depth >= 2, issue #13715 link) is removed. The Claude fetch skill is untouched.
- The active fetch-path-composition spec is rebalanced from hang severity to prompt hygiene by the delta spec in this change; archived changes are not rewritten.
- Dual `subagent_depth` 2 plus doctor and installer depth >= 2 checks are unchanged (retained for v1 compatibility); the staged diff contains no config, doctor, installer, or SAI_LEARNINGS change, so those retentions are carried as unchanged boundaries, not as new normative behavior.

## Capabilities

### New Capabilities

None — no new product capability was implemented. ADR 0185 is an amendment record, not a product capability.

### Modified Capabilities

- fetch-path-composition: the opencode fetch skill no longer carries the #13715 load-bearing severity note; the path-composition rule stays as hygiene with its observed dropped-segment evidence.

## Impact

New files:

- `docs/adr/0185-remove-hang-rationale-from-sibling-dispatch.md` — amendment record retiring the hang citation, keeping the sibling topology and the v1/v2 note.

Modified files:

- `docs/adr/0000-INDEX.md` — 0185 command-group entry and relation-table `amends` row for 0172d.
- `skills/opencode/fetch/SKILL.md` — hang sentence removed; path-composition rule and observation kept.
- `openspec/changes/amend-0172d-remove-hang-rationale/specs/fetch-path-composition/spec.md` — MODIFIED delta rebalancing the severity note to hygiene (this change's spec artifact).

Known limitations and debt carried forward: coordinator sequencing stays slightly more explicit than a single mega-dispatch; the v1 compatibility note is carried so v1 users are not misled; depth stays within the demonstrated envelope with no new permission-scope territory opened.

Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
