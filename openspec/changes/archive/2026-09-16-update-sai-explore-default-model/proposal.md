> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.

**Complexity**: low (2 files, no breaking change)

## Why
The opencode sai-explore default (opencode/muse-spark-1.3-contributor-free with high) no longer matched the desired orchestrator routing. The change aligns explore with the opencode-go provider while keeping a free spark tier, as evidenced by the staged wrapper and README updates.

## What Changes
- commands/opencode/sai-explore.md frontmatter model changes from opencode/muse-spark-1.3-contributor-free to opencode-go/muse-spark-1.3-contributor-free.
- The same wrapper frontmatter variant changes from high to xhigh.
- README.md recommended-models explore row changes to the same model and variant, keeping docs in sync with the wrapper.
- No Claude wrapper change; no behavior change besides default model routing.

## Capabilities
### New Capabilities
- sai-explore-default-model — opencode explore wrapper default routing plus its README documentation row.

### Modified Capabilities
- None — no existing capability spec behavior was modified in this change; the opencode-default-models SHALL norm was left untouched and the divergence is recorded as debt.

## Impact
- Modified: commands/opencode/sai-explore.md (frontmatter model and variant).
- Modified: README.md (explore row model and variant).
- Operational note: the installed global wrapper reflects the change only after reinstall; a project-local override still wins.
- Known divergence: explore now diverges from the cheap-group convention (opencode plus high) and from the current SHALL norm in opencode-default-models.
- Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill

## Proposal Research Documentation
- commands/opencode/sai-explore.md frontmatter declaring the implemented model and variant.
- README.md recommended-models table explore row documenting the same values.
- openspec/specs/opencode-default-models/spec.md cheap-path SHALL norm, evidencing the divergence.
- agents/opencode/explore.md free-model frontmatter, reviewed as context only and left unchanged.

## Additional Notes
- Scope was limited to commands/opencode/sai-explore.md plus the README row; agents/opencode/explore.md and other cheap wrappers were explicitly out of scope.
- Rejected alternatives (non-normative): switching agents/opencode/explore.md, switching all cheap wrappers, keeping variant high.
- Limitations and debt: installed wrapper requires reinstall to take effect; spec divergence stands unless opencode-default-models is updated or an exemption is recorded; variant high to xhigh changes orchestrator effort versus remaining cheap paths on high.
