> **⚠ POST-HOC RECORD** — This proposal was backfilled after implementation against a user-supplied statement of intent. It describes a decision already made, not one being proposed.
## Why
Citations such as `item 8` and `(items 8 and 9)` in the explore crystallization steps read as global `instructions.md` items while meaning step-local numbers, sending readers to the wrong rule. The staged implementation rewrites those citations by name so each reference resolves to the intended crystallization protocol, language gate, or selector description without changing behavior.
## What Changes
Rewrote ambiguous file-local `item N` citations by name across four step files: clarified the step-local `8.` crystallization language gate header and its machinery comparison in `crystallization-language-gates.md`; replaced `item 8`, `item 5 or 6`, `gate 9`/`item 10`, and `item 7` shorthands with named protocol and gate references in `crystallization-protocol.md`; replaced the stale `(items 8 and 9)` parenthetical with the earlier two-option selector description in `pipeline-direct-build.md`; and replaced `per item 8` and related shorthands with the named crystallization language gate in `route-selector.md`. Global `instructions.md:4-11` numbering is untouched.
## Capabilities
### New Capabilities
- disambiguate-item-refs — unambiguous named citations for step-local crystallization references.
- fix-stale-selector-ref — corrected Direct Build selector inheritance clause with no valid-referent parenthetical removed.
### Modified Capabilities
- None — all changes are new disambiguation wording; no existing capability behavior was modified.
## Impact
- Modified: `sai/commands/explore/steps/crystallization-language-gates.md`
- Modified: `sai/commands/explore/steps/crystallization-protocol.md`
- Modified: `sai/commands/explore/steps/pipeline-direct-build.md`
- Modified: `sai/commands/explore/steps/route-selector.md`
Out of scope: design.md, tasks.md, implementation.md — not generated by /sai-backfill
