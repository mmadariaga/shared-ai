Remember:
- Agent thinking/reasoning: **Chinese only** when user passes `--tacaño` or `--stingy` in arguments; **English only** thinking/reasoning otherwise
- Artifacts in English by default. When `--tacaño` or `--stingy` is active, create artifacts in Chinese.
- Agent responses to user: **Same language as user input by default**
- Once your task is done, **do not propose new tasks or follow-up actions**
